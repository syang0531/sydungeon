# Changelog

## 0.6.0 (2026-09-21)

**마녀의 늪** — 늪·맹그로브 늪, spacing 36 / separation 12. **물 위 말뚝 마을과 그 밑 양조장.**

- 가운데 **큰 오두막**에서 널다리가 사방으로 뻗고 끝마다 작은 오두막이 달린다. 상자 오두막,
  마녀 오두막, 슬라임 오두막
- 큰 오두막 바닥의 구멍으로 **사다리가 물을 뚫고 21칸** 내려가면 진흙 벽돌 양조장이다.
  양조실·증류실·물이 찬 드라운드 방
- 보스 **늪의 어머니**(마녀, 체력 90)는 통로 셋 뒤 21×14×21 홀 한가운데 물웅덩이의 섬에
  선다. 사분면 경비 넷이 마녀·슬라임·드라운드 웨이브
- **시그니처(확정): 양조기 + 네더 사마귀 8 + 블레이즈 가루 4.** 네더에 가기 전에 양조가
  열리는 유일한 길이다. 그 위에 마법 부여(20~28) 다이아 장비 1과 유리병

**위아래가 서로 다른 기계다.** 물 위는 이 모드에서 직소를 가장 느슨하게 쓰는 곳이다 —
지킬 형태도 채울 칸도 없이 마을처럼 자라고, 끝은 난간 한 장으로 막힌다. 물 밑은 지하감옥
그대로다. 둘을 잇는 것은 직소가 아니라 사다리 한 줄이다.

**물 위에 서는 법** (`CLAUDE.md` §17). 늪의 구조물은 물 위 첫 빈 칸에서 시작하므로 덱을
세 켜 올리고 말뚝을 내린다. **덱 아래는 말뚝 말고 아무것도 쓰지 않는다** — 안 쓴 자리는
월드가 갖고 있던 물이 남는다. 수직통로만 정반대로 일부러 공기를 쓴다(물을 밀어내야 하니까).
`terrain_adaptation`은 `none`이다.

천장 구멍을 직소보다 먼저 파야 한다는 것을 또 잊어서 지하 허브의 입구가 지워졌다. 피라미드
사다리에서 한 번 겪은 함정이다 (§11).

**조각은 코드가 지은 첫 판이다.** `python tools/workshop.py swamp`로 전부 깔린다.

## 0.5.0 (2026-09-21)

**마법사의 탑을 사람이 지었다.** 0.3.0(39×39 직육면체)과 0.4.0(가는 탑)은 둘 다 코드가
지었고 둘 다 "외형과 인테리어가 아쉽다"는 평을 받았다. 이번엔 개발 클라이언트에 직접 층
배치도를 그리고, 코드는 그걸 자르고 배선만 했다.

- **둥근 탑.** 밑변 29, 높이 56, 여덟 층. 21×21 방 덩어리의 네 꼭짓점이 지름 29 원 위에
  놓이고 그 사이를 4칸 테두리가 메운다 — 동서남북은 두껍고 대각선은 없다
- **한 층이 7×7 방 3×3 = 아홉 칸, 여덟 층이니 일흔두 칸.** 빈 칸이 하나도 없다
- **층마다 계단방이 다른 칸에 있다.** 매 층 걸어서 찾아야 한다. 계단은 손으로 지은 나선이고
  위아래가 둘 다 서쪽으로 열려서, 올라가면 들어간 칸 바로 위로 나온다
- **비밀방** — 문이 하나도 없고 상자만 있다. 이웃의 문이 그 벽에 붙어 **돌로 막힌 문**으로
  보이고, 층이 한 칸 모자란 것을 눈치채고 파야 찾는다
- **4층 도서관** 마법 부여대와 유효 책장 열다섯, **8층 성소**에 대마법사와 불 붙이지 않은
  포탈 틀. 거리에는 스포너, 방에는 상자
- 타이가 셋에 자연 생성 (spacing 40 / separation 14)

**미로를 자라게 두지 않는다.** 직소 생성은 문에서 문으로 자라서 어떤 칸이 생긴다는 보장이
없는데, 칸 하나가 곧 방인 탑에서는 빈 칸이 구멍이다. 그래서 `core`가 칸마다 직소를 하나씩
갖고 일흔두 칸을 전부 직접 놓는다. 이어지는 것은 층마다의 **경로**가 보장한다
(`CLAUDE.md` §14). 그리고 **경로가 층을 다 덮으면 안 된다** — 통로는 방이 될 수 없어서,
다 덮으면 보상 방도 비밀방도 설 자리가 없다 (§15).

**새 도구.** `scan_world.py`가 월드 region에서 블록을 그대로 읽고, `grab_cells.py`가 층
배치도를 격자대로 잘라 조각으로 만든다. `workshop.py`는 조각을 전부 SAVE 설정된 구조물
블록과 함께 깔아 준다. `import_piece.py`는 손으로 고친 조각을 **모드에 있던 것의 배선과
대조**해서 되가져온다.

**고친 것.** `structure_void`를 조각에 넣으면 **진짜 블록으로 박힌다** — 안 보이고 통과되는
블록이라, 둥근 탑 발치의 잔디를 덮어써서 그 아래 흙이 드러났다. 템플릿은 목록에 있는 블록만
놓으므로 "손대지 마라"는 뜻은 목록에서 빼는 것이다. 피라미드 껍데기 셋도 같은 것을 들고 있었다
(가운데라 덮여서 안 보였을 뿐이다).

**문서.** CurseForge 등록 정보가 0.1.0 카피 그대로여서 "다음은 대피라미드와 마법사의 성"이라고
광고하고 있었다. 세 던전을 다 담아 다시 썼다. 콘셉트·설계·레벨 문서의 낡은 부분도 같이 고쳤다.

**0.3.0과 0.4.0은 배포하지 않았다.** 둘 다 코드가 지은 탑이고 둘 다 갈아엎었다.

**0.6.0으로 미룬 것:** 지붕, 테두리 꾸미기, 조각 종류 늘리기.

## 0.4.0 (2026-09-21) — 미배포

**마법사의 탑을 다시 지었다.** 0.3.0의 탑은 39×39가 56칸까지 그대로 올라가는 직육면체였고,
사다리 하나로 곧장 꼭대기까지 갔다. 참고할 탑 사진 넷을 받고 외형부터 손봤다.

- **실루엣.** 받침 39폭(1층) → 몸통 **25폭**(2~8층) → 왕관 **39폭**(몸통보다 일곱 칸씩
  튀어나온 전망 회랑) → 산화 구리 지붕이 한 칸씩 좁아지다 피뢰침. **총 높이 86.**
  재질은 이끼 낀 돌과 산화 구리, 가문비나무 난간
- **사다리를 없앴다.** 층마다 계단방이 하나씩이고, 그 방이 **층마다 다른 칸**에 있다.
  착지한 자리에서 계단까지 걸어가야 하고 그 경로가 층마다 꺾인다
- **미로에도 계단이 섞였다** (`stair_maze`, 가중치 5). 정해진 길 말고 층을 건너뛰는
  지름길이 매번 다르게 난다 — 지하감옥의 `stair`와 같은 생각이다
- **도서관과 성소가 21³ → 14³**(2×2칸 × 두 층). 3×3 층에서 2×2를 쓰면 바깥에 ㄱ자로 다섯
  칸이 남고, 거기에 계단이 선다. 21³이면 층 전체를 먹어서 올라갈 자리가 없었다
- 1층은 현관 홀 → 복도 → 연금실 → 대계단이 전부 고정 사슬이다. 문이 돌벽에 막히지 않는다
- 8층 계단이 **옥상 회랑**으로 나간다. 상자 하나와 사방 전망
- 껍데기가 넷이 되면서 직소 깊이가 30~38로 늘었고 `MAX_DEPTH`를 64로 올렸다

**레벨 설계 문서** — `docs/levels/`. 던전마다 조각을 **층마다 한 장씩 블록 단위로** 그리고,
직소가 보는 방향을 표시하고, 조각들이 어떤 규칙으로 이어 붙는지를 적었다.
`tools/gen_level_doc.py`가 게임이 읽는 파일에서 그대로 읽어 만들기 때문에 모드와 어긋날 수 없다.

## 0.3.0 (2026-09-21) — 미배포

**마법사의 탑** — 타이가 셋에만, spacing 40 / separation 14. 밑변 39×39, 높이 56. **일곱 층, 층마다 7×7 방 스물다섯 칸.**

- 서쪽 계단실이 층마다 하나씩 쌓이고 사다리가 1층 바닥부터 꼭대기까지 통한다. 1층 동쪽 문은 연금실, 2층은 **도서관**, 5층은 **성소**. 전부 원소 하나뿐인 풀이라 반드시 한 번 놓이고 항상 계단실에서 바로 들어간다
- **도서관 21×21×21** — 3층 높이 아트리움. 벽면 전체 책장, 사다리로 오르는 회랑 둘, 바닥 단상의 마법 부여대에 유효 책장 15개
- **성소 21×21×21** — 대마법사(이보커, 체력 100)와 불 붙이지 않은 흑요석 포탈 틀
- 보스 보상(확정): **엔더의 눈 4 + 불사의 토템 + 마법 부여(25~30) 다이아 장비 1**
- `core`가 층마다가 아니라 탑 전체에 **상자 하나**다. 그래야 21칸 높이 방이 가운데 서고 미로가 둘레로 흐른다
- 감옥이 타이가 셋을 넘겨 10개 바이옴이 됐다

**처음엔 "마법사의 성"으로 만들었다가 갈아엎었다.** 플레이 테스트에서 **보스방과 관측소에 들어가는 길이 아예 없었다.** 한 조각에 같은 이름의 직소를 여럿 달아서, 바닐라가 뒤집어 붙이면 사슬이 거꾸로 갔다. 이제 직소 이름을 역할로 나누고(`tow_door`/`tow_riser`/`tow_anchor`/`tow_placer`) `verify()`가 입구 이름의 개수를 센다 (`CLAUDE.md` §13). 허공에 뜬 철창과 벽에 박힌 유리창도 같이 고쳤다.

## 0.2.0 (2026-09-20)

**대피라미드** — 사막에만, spacing 48 / separation 20. 밑변 91×91, 높이 46의 45° 사암 피라미드.

- 지표의 입구에서 9×9 칸 미로로 들어가고, 중앙 우물의 사다리가 21×21×21 묘실로 올린다. 묘실 천장 위에는 석관실
- **형태가 보장된다.** 껍데기 세 장이 결정적 수직 사슬로 쌓이고, 미로는 `core` 조각의 바운딩 박스 안에 갇혀 경사면을 뚫지 못한다 (`CLAUDE.md` §10). 차단 조각이 필요 없다
- 칸 종류: 통로·모퉁이·교차로·전리품 니치에 더해, 허스크 스포너가 든 모래 소굴과 교차로 초소, 솔질하는 수상한 모래 발굴터
- **밟는 함정은 넣지 않는다.** 압력판 TNT 방과 모래 천장 함정을 만들었다가 뺐다. 압력판도 철사도 플레이어를 가리지 않아서, 던전을 돌아다니는 몹이 플레이어보다 먼저 밟는다. TNT 방은 아무도 없을 때 터져 **피라미드 일부를 날려버렸다**. 남은 위험은 트리거가 필요 없는 것들이고, 수상한 모래는 솔질이 플레이어만 할 수 있는 유일한 상호작용이라 남았다
- 보스는 파라오(허스크, 체력 80, 금 장비)와 경비 웨이브. 클리어 보상은 **흑요석 14 + 화염과 강철 + 마법 부여 다이아 장비 1개 확정** — 앞의 둘은 네더 포탈과 마법 부여대, 마지막은 "보스당 한 조각" 규칙
- `tools/generate_pyramid.py`가 조각과 풀을 만들고 기하를 자체 검사한다. 구조물 블록은 한 변 48칸이 한계라 91칸 껍데기는 게임에서 저장할 수 없다
- 검사기가 조각 팔레트의 블록 이름도 대조한다. CI가 소스 jar를 항상 풀지는 않아 v0.1.1 배포가 한 번 실패했고, 이제 항상 생성되는 jar를 쓴다

## 0.1.1 (2026-09-20)

지하감옥 마무리. 모드의 목적이 "파밍과 채굴 없이 탐험만으로 엔드 콘텐츠"라는 것을 `docs/concepts.md`에 세우고, 첫 던전이 그 약속의 첫 단추를 채우도록 고쳤다.

- **생성 바이옴을 육지 전체에서 온대·냉대 숲과 평원 13종으로 축소.** 바이옴 하나에 던전 하나가 원칙이다 (`docs/concepts.md` §2.7). 어두운숲·창백한 정원은 저주받은 묘지, 눈 타이가·그로브는 얼음 성채 몫으로 넘겼다
- **상자에 침대·횃불·식량 추가.** 두 테이블 모두 풀이 둘이 되어 첫 풀이 생존 물자 하나를 확정으로 준다. 감방 = 횃불·빵·구운 돼지고기·석탄·침대·양초, 경비실 = 횃불·구운 소고기·구운 양고기·침대·랜턴. 경비실 둘째 풀에 석탄·황금 당근도 넣었다
- **`tools/validate_data.py`**: 데이터팩의 모든 id를 게임 jar와 대조하고 조각·풀·전리품·스포너의 상호 참조를 검사한다. 0.1.0에서 `minecraft:chain`(26.2에서는 `iron_chain`)이 감방 전리품 테이블 전체를 로드 실패시켜 상자가 비어 있었고, `minecraft:dappled_forest`(26.3 전용)가 월드 로딩을 막았다. 둘 다 빌드는 통과했었다
- 바이옴별 던전 16종과 T0~T5 보상 설계를 `docs/concepts.md`에 문서화

## 0.1.0 (2026-09-20)

- 지하감옥 조각 12개 (`structure/dungeon/`): entrance, shaft, hub, shaft_cap, passage, passage_cell, passage_cells, cross, stair, dead_end, cell, cap
- 풀 6개 (start / shafts / shaft_caps / passages / cells / caps), 모든 풀의 fallback은 1칸 마개
- `sydungeon:dungeon` 구조물: 지표 입구에서 사다리로 내려가는 미로. 깊이 13~23 랜덤(`sydungeon:ranged_jigsaw`), beard_thin, 육지 바이옴
- 전리품 상자 2종(`chests/dungeon_cell`, `chests/dungeon_guard`), dead_end 스포너(빛 무시)
- 풍화 processor `dungeon_weathering`: 금간·이끼 벽돌, 조약돌, 빠진 창살
- 보스방: hub 남문에서 보스 통로 최소 5개 뒤에 21×21×21 방. 트라이얼 스포너 다섯(좀비말 탄 간수장 + 사분면 경비 웨이브), 클리어 시 플레이어마다 마법 부여 다이아 장비 지급(`trial/boss_reward`)
- 입구와 보스방 외의 랜턴 제거. 교차로 스포너 조각 `cross_guard`, 경비실 가중치 5
- CurseForge 등록 정보(`docs/curseforge/`), 로고, `PUBLISHING.md`
- Minecraft 26.2 / NeoForge 26.2.0.88. 26.3 월드에서 저장한 연습 조각은 변환기가 26.2 형식으로 내림
- 도구: `tools/nbt.py`, `convert_yame.py`, `dump_structure.py`, `pack_datapack.py`

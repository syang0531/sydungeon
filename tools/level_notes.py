# -*- coding: utf-8 -*-
"""Prose for the level-design documents. `gen_level_doc.py` draws everything else.

`INTRO[<family>]` is the head of the document:

    title      the H1
    summary    section 1 - what this dungeon is, and what one run of it feels like
    principle  section 2 - the rule that makes it hold together
    skeleton   the paragraph above the assembly picture (only for families that have one)
    floors     ('label', y) for the assembly's floor plans
    order      the order the pieces are written in; anything left out follows alphabetically
    outro      the tail - what is still rough

`NOTES['<family>/<piece>']` is the paragraph under one piece's heading. Everything a reader
could count off the picture - sizes, jigsaws, block tallies - is drawn, not written, so a
note says only what the picture cannot: why the piece exists and what it must not lose.
"""

INTRO = {}
NOTES = {}


# =========================================================================================
INTRO['dungeon'] = {
    'title': '# 레벨 설계 — 지하감옥',

    'summary': """
숲과 평원의 지표에 돌벽돌 정자가 서 있고, 그 바닥 구멍으로 사다리가 내려간다. 수직통로를
두세 번 갈아타면 허브에 닿고, 거기서 미로가 시작된다. 미로는 7×7×7 칸을 격자처럼 이어
붙인 것이고, 통로 옆 철창 뒤에는 감방이, 막다른 방에는 경비실과 스포너가 있다. 허브의
남문 하나만은 미로가 아니라 **보스 가지**로, 통로를 다섯 개 지나야 21×21×21 보스방이 나온다.

**한 판:** 지표 정자 → 사다리 21~63칸 → 허브 → 미로(조각 300~1000개, 5~8층) →
어딘가의 보스방. 횃불과 침대는 상자에서 나온다 — 그게 이 던전이 처음으로 하는 약속이다.
""",

    'principle': """
### 격자 하나, 커넥터 하나

지하감옥은 셋 중 **가장 단순한 원리**로 만들어져 있다. 지하에 있으니 **겉모양을 지킬 필요가
없다.** 미로가 어디로 자라든 위에서 보이지 않는다. 그래서 형태를 가두는 장치가 아예 없고,
조각은 그냥 사방으로 자란다.

그 대신 규칙은 둘뿐이다.

1. **모든 조각은 7×7×7의 배수**이고, 출입구는 면 중앙 3폭×4높이, 직소는 그 면 바닥 중앙에
   있다. 그래서 어느 조각이 어느 조각에 붙어도 문이 문에 맞는다.
2. **미로의 직소는 이름이 전부 `door` 하나**다. 무엇이 붙을지는 직소가 가리키는 **풀**이
   정한다 — `passages`를 가리키면 통로, `cells`를 가리키면 감방.

두 조각이 같은 칸으로 수렴하면 바닐라가 충돌을 잡아내고 그 가지를 **fallback 풀**로 넘긴다.
모든 풀의 fallback은 `caps`, 즉 **두께 1짜리 벽 한 장**이다. 두께가 1이라 거의 언제나 들어가고,
그래서 "허공으로 뚫린 문"이 남지 않는다. 벽으로 막힌 문은 그냥 **막아둔 문**처럼 보인다.

### 형태를 안 지키는 대신, 있어야 할 것을 사슬로 건다

무작위에 맡기면 안 되는 것이 둘 있다.

- **미로가 지표에 붙는 것** — 입구 바로 아래 직소는 `shaft_first`(수직통로만 든 풀)를 부른다.
  이게 없으면 40%는 입구 바로 밑이 허브여서 미로가 지표 높이에 생겼다.
- **보스방이 안 나오는 것** — 허브 남문이 `boss_approach_1`을 부르고, 그 풀의 유일한 원소인
  `boss_passage_1`의 동문이 `boss_approach_2`를 부르고… `boss_approach_5`까지 간 뒤에야
  보스방이 나올 수 있는 풀에 닿는다. **"최소 다섯 칸"을 강제하려면 같은 통로의 복사본이
  다섯 개 필요하다.** 그리고 보스 가지의 직소는 `placement_priority` 10이라 미로보다 먼저
  놓인다 — 3×3×3짜리 방은 미로가 먼저 자라면 놓을 자리를 잃는다.

### 왜 보스 조각만 커넥터 이름이 다른가

바닐라는 자식 조각의 **아무 직소나** 이름이 맞으면 입구로 쓰고, 회전 넷을 다 시도한다.
십자형인 `boss_passage`를 전부 `door`로 배선하면 부모가 "다음 보스 통로로 나가는 문"으로
들어와 버려서 사슬이 거꾸로 간다. 그래서 보스 조각의 **입구만** `boss_door`이고, 보스 가지를
잇는 직소의 target이 `boss_door`다. 이 규칙을 마법사의 성에서 잊었다가 갈아엎었다.
""",

    'order': ['entrance', 'shaft', 'shaft_cap', 'hub',
              'passage', 'passage_cell', 'passage_cells', 'cell',
              'cross', 'cross_guard', 'stair', 'dead_end', 'cap',
              'boss_passage_1', 'boss_passage_2', 'boss_passage_3', 'boss_passage_4',
              'boss_passage_5', 'boss_passage', 'boss_room'],

    'outro': """
## 다듬을 곳

플레이 테스트에서 이 던전은 "괜찮다"는 평을 받았다. 그래도 그림을 놓고 보면 남는 것들이 있다.

- **`stair`의 두 문이 같은 서쪽 면에 있다.** 어느 쪽으로 붙느냐가 무작위라 절반은 아래층으로
  이어진다. 의도한 것이긴 한데, 계단 위아래가 같은 방향이라 층을 옮겼다는 감각이 약하다.
- **감방(`cell`)은 상자 하나와 빈 바닥뿐이다.** 침상·사슬·낙서 같은 것이 없다. 7×7 칸이라
  자리는 있다.
- **`passage`는 완전히 비어 있다** (돌벽돌 257 + 직소 2). 미로의 대부분이 이 조각인데
  아무것도 없다. 바닥 무늬나 벽감 정도는 들어갈 만하다.
- **보스방 천장이 21칸으로 높지만 위쪽이 비어 있다.** 쇠사슬 랜턴 8개 말고는 공중에 아무것도
  없다.
""",
}


NOTES['dungeon/entrance'] = """
유일하게 지표에 서는 조각이고, `project_start_to_heightmap`으로 지형 높이에 맞춰 놓인다.
지붕 있는 정자 — 동서남이 트여 있어 멀리서 보인다. 바닥의 3×3 구멍(x 2–4, z 1–3)이
사다리의 시작이고, **가운데가 아니다.** 사다리가 z=1에서 북벽에 걸려야 하기 때문이고,
그래서 수직통로 사슬의 모든 조각은 북벽이 막혀 있어야 한다.
"""

NOTES['dungeon/shaft'] = """
7×21×7, 즉 **셀 세 칸 높이**의 사다리 기둥. 위아래 직소가 둘 다 `joint=aligned`라 회전하지
않고, 그래서 사다리가 내려가는 내내 같은 벽에 붙어 있다. `shaft_first` 풀에 혼자 들어가 있어서
입구 바로 아래에는 반드시 이 조각이 온다.
"""

NOTES['dungeon/shaft_cap'] = """
높이 1짜리 바닥. 수직통로가 깊이를 다 써서 허브에 못 닿았을 때 구멍을 막는다. 없으면
사다리가 허공에서 끝난다.
"""

NOTES['dungeon/hub'] = """
사다리가 닿는 방이고 미로의 시작점이다. **북문이 없다** — 북벽은 사다리가 걸리는 벽이라
비워 둘 수 없다. 서·동문이 미로로, 남문이 보스 가지로 간다. 남문 직소만
`placement_priority`·`selection_priority`가 10이라 보스방이 미로보다 먼저 자리를 잡는다.
"""

NOTES['dungeon/passage'] = """
미로의 기본 단위. 서-동 직선. 가중치 10으로 가장 흔하다.
"""

NOTES['dungeon/passage_cell'] = """
남쪽 벽이 철창이고 그 너머 직소가 `cells` 풀을 부른다. **철창은 문이 아니다** — 감방에
들어가려면 깨야 한다. 상자를 공짜로 주지 않으려는 장치고, 이 던전에서 곡괭이의 쓸모는
사실상 이것뿐이다.
"""

NOTES['dungeon/passage_cells'] = """
양쪽이 철창. 통로 하나로 감방 둘을 매단다.
"""

NOTES['dungeon/cell'] = """
북쪽 면 **전체**가 열려 있다 (x 1–5, y 1–4, 20칸). 부모의 철창 벽에 그대로 붙는 면이라
문 규격(3폭)이 아니라 넓게 뚫려 있다. 안쪽 벽에 상자 하나 — `chests/dungeon_cell`.
"""

NOTES['dungeon/cross'] = """
십자 교차로. 미로가 갈라지는 자리라 가중치가 통로와 같은 10이다.
"""

NOTES['dungeon/cross_guard'] = """
교차로 한가운데 몬스터 스포너. 던전이 어둡기 때문에 이 스포너가 실제로 작동한다
(`custom_spawn_rules.block_light_limit 0..15`). 밝았다면 장식이었다.
"""

NOTES['dungeon/stair'] = """
7×14×7 — 셀 두 칸을 쓰면서 한 층(7칸)을 올린다. 층 간격이 정확히 7이라 위층도 같은 격자에
맞는다. **두 직소가 둘 다 서쪽 면**이라 부모가 어느 쪽으로 붙느냐에 따라 올라가는 계단도,
내려가는 계단도 된다. 가중치 5 — 던전이 5~8층이 되는 건 이 조각 하나 때문이다.
"""

NOTES['dungeon/dead_end'] = """
경비실. 가운데 스포너(좀비·스켈레톤·거미·동굴거미)와 구석에 상자 `chests/dungeon_guard`.
문이 하나뿐이라 들어가면 돌아 나와야 한다. **fallback으로는 쓰지 않는다** — 7칸을 통째로
쓰기 때문에 최대 깊이에서는 충돌해서 구멍이 남는다. 그 자리는 `cap`이 맡는다.
"""

NOTES['dungeon/cap'] = """
**두께 1의 벽 한 장.** 모든 풀의 fallback이고, 던전에서 가장 자주 놓이는 조각이다.
얇아서 최대 깊이에서도 거의 항상 들어간다. 이 조각이 없으면 미로의 끝마다 흙과 동굴이
드러난다.
"""

for _i in range(1, 5):          # _5 is the last link and says something different
    NOTES['dungeon/boss_passage_%d' % _i] = """
보스 가지의 %d번째 칸. `cross`에서 파생했지만 **서문만 이름이 `boss_door`**이고, 동문의
target이 `boss_door`다. 부모는 서문으로만 들어올 수 있고 사슬은 서→동 한 방향으로만 흐른다.
동문이 부르는 풀은 `boss_approach_%d` — 그 풀에 든 조각은 하나뿐이라 **반드시** 다음 칸이 온다.
북·남문은 평범한 `passages`라 보스 가지도 미로를 낳는다.
""" % (_i, _i + 1)
NOTES['dungeon/boss_passage_5'] = """
보스 가지의 다섯 번째 칸. 동문이 부르는 `boss_approach`에는 `boss_passage` 2 : `boss_room` 1이
들어 있어서, 여기서부터는 방이 나올 수도 있고 통로가 더 이어질 수도 있다. 그래서 보스방은
허브에서 **최소 다섯, 보통 다섯~여덟 칸** 뒤다.
"""

NOTES['dungeon/boss_passage'] = """
`boss_approach` 풀의 통로 쪽. 자기 자신을 다시 부르기 때문에 보스방까지의 거리가 매번 다르다.
"""

NOTES['dungeon/boss_room'] = """
21×21×21 — **3×3×3 셀**. 미로가 먼저 자라면 놓을 자리가 없으므로 보스 가지 전체가
우선순위 10으로 먼저 놓인다. 입구는 서쪽 면 **정중앙**(z 9–11) 하나뿐이다.

단상 위에 간수장 트라이얼 스포너 하나, 사분면마다 경비 스포너 넷. **클리어 판정과 보상은
트라이얼 스포너가 한다** — 상자는 싸우지 않고 열 수 있지만 트라이얼 스포너는 웨이브를 다
잡아야 `loot_tables_to_eject`를 뱉고, 싸운 플레이어마다 따로 준다. 쿨다운이 사실상 무한이라
던전당 한 번이다. 랜턴이 있는 두 조각 중 하나다(다른 하나는 입구).
"""


# =========================================================================================
INTRO['pyramid'] = {
    'title': '# 레벨 설계 — 대피라미드',

    'summary': """
사막 한가운데 밑변 91칸, 높이 46칸의 45° 사암 피라미드가 서 있다. 북면의 문으로 들어가면
9×9칸(63×63블록) 미로가 있고, 미로 한가운데 **우물**의 사다리가 천장을 뚫고 21×21×21
**묘실** 바닥 한복판으로 올려보낸다. 올라서는 순간 스포너 다섯에 둘러싸인 채로 파라오와 싸운다.
묘실 천장 구멍 위에는 석관실이 하나 더 있다.

**한 판:** 북면 입구 → 스파인 3칸(결정적) → 우물 → 미로를 돌며 전리품·수상한 모래·허스크 →
우물로 돌아와 사다리 → 묘실에서 보스 → 천장 해치 → 석관실.

**클리어 보상이 흑요석 14 + 화염과 강철 + 마법 부여 다이아 장비**다. 네더 포탈과 마법
부여대를 여기서 받는다.
""",

    'principle': """
### 지하감옥과 다른 점은 **겉모양이 있다**는 것 하나다

지하감옥은 어디로 자라도 상관없었다. 피라미드는 **45° 경사면이 무너지면 안 된다.** 그런데
직소 생성은 실루엣이라는 걸 모른다. 그냥 두면 미로가 경사면을 뚫고 나가 사막에 구멍이 뚫린다.

막는 방법은 세 가지가 있었다.

1. 차단 조각을 둘러 세운다 — 가장자리 칸마다 "여기서 멈춰" 조각을 놓아야 하는데, 직소는
   위치를 모르므로 강제할 수가 없다.
2. `structure_void`로 바깥을 채운다 — 충돌 판정에 영향을 주지 않아서 소용이 없다.
3. **부모의 바운딩 박스를 울타리로 쓴다** ← 이걸 택했다.

### 상자가 형태를 지킨다

`JigsawPlacement`에 규칙이 하나 있다. **직소가 자기 조각의 바운딩 박스 *안쪽*을 가리키면,
그 자식들은 전역 공간이 아니라 그 박스를 여유 공간으로 삼아 판정된다.** 네더 요새(bastion)가
큰 방을 작은 방들로 채우는 방식이고, 여기서는 그대로 울타리가 된다.

```
skin_base (91×7×91)  ─안쪽→ core (63×7×63) ─안쪽→ 스파인 → 미로 전체
skin_mid  (77×21×77) ─안쪽→ tomb (21×21×21)
skin_top  (35×18×35) ─안쪽→ crypt (7×7×7)
```

미로 조각은 전부 `core`의 자손이므로 **전부 core의 박스로 판정된다.** core는 피라미드
가장자리에서 8블록 안쪽이라 경사면은 아예 건드려지지 않는다. 차단 조각도, 관리할 void도
필요 없다 — **박스가 울타리다.**

껍데기 세 장은 `joint=aligned` 수직 직소 하나짜리 사슬로 쌓이므로 피라미드는 항상 온전하다.

### 결정적인 기하는 직소를 쓰지 않는다

입구 터널, 우물에서 묘실로 오르는 사다리, 묘실에서 석관실로 가는 해치 — 이것들은 **두 조각에
같은 좌표로 새겨** 놓았다. 앵커 직소 하나로 이어진 두 조각은 언제나 같은 상대 위치에 놓이기
때문에, 양쪽에 같은 구멍을 파면 그게 곧 통로가 된다. `generate_pyramid.py`의 `AT`/`SIZE` 표가
유일한 출처이고 `verify()`가 매번 검사한다.

> **순서 함정:** 천장 구멍을 사다리보다 **나중에** 파면 사다리 맨 윗칸이 지워진다. 한 번 그랬다.

### 커넥터 이름도 역할로 나뉜다

`pyr_door`는 미로 커넥터, `pyr_spine`은 스파인 사슬, `pyr_anchor`는 결정적으로 놓이는 큰
조각의 입구다. **`pyr_anchor`를 가진 조각은 그 직소가 딱 하나다** — 부모가 다른 문으로
들어오는 사고를 막는다.

### 밟는 함정은 없다

압력판도 철사도 플레이어를 가리지 않는다. 던전에는 스포너와 자연 스폰으로 몹이 돌아다니므로
**플레이어가 오기 전에 몹이 전부 밟는다.** 0.2.0 첫 판에서 TNT 함정방은 아무도 없을 때 터져서
**피라미드 일부를 같이 날려버렸다.** 지금 남은 위험은 트리거가 필요 없는 것들뿐이다 — 스포너,
그리고 **솔질은 플레이어만 할 수 있는** 수상한 모래.
""",

    'skeleton': """
껍데기 세 장이 수직으로 쌓이고, `core`·`tomb`·`crypt`가 각각의 껍데기 안쪽에 앵커로 매달리고,
스파인 셋이 입구에서 우물까지 이어진다. **여기까지는 매번 똑같다.** 미로는 `well`과
스파인의 옆문에서 자라는 것이라 이 그림에 없다.
""",

    'floors': [('미로 바닥', 1), ('묘실 바닥', 9), ('묘실 위쪽', 25), ('석관실', 30)],

    'order': ['skin_base', 'skin_mid', 'skin_top', 'core',
              'spine_1', 'spine_2', 'spine_3', 'well', 'tomb', 'crypt',
              'passage', 'corner', 'cross', 'niche',
              'sand_den', 'dig_site', 'guard_post', 'cap'],

    'outro': """
## 다듬을 곳

- **껍데기가 통짜 사암이다.** 층마다 깎은 사암 띠가 하나 들어갈 뿐, 45° 면에 아무 무늬가 없다.
  실물 피라미드처럼 면에 얕은 층을 주거나, 모서리 선을 다른 블록으로 긋는 것이 어울린다.
- **입구가 북면 한가운데에 문 하나뿐이다.** 스핑크스나 계단, 오벨리스크 같은 외부 장식이 없어서
  멀리서는 그냥 모래 산으로 보인다.
- **미로 칸이 전부 평평하다.** 피라미드 1층은 높이 7칸 한 층뿐이라 위아래가 없다. 지하감옥의
  `stair`에 해당하는 것이 없다.
- **묘실 위쪽 절반이 비어 있다.** 21칸 높이인데 단상과 스포너는 바닥에 있고 천장 근처는
  랜턴 넷뿐이다.
- **`dig_site`의 수상한 모래가 4칸뿐**이라 솔질 한 번이면 끝난다.
""",
}

NOTES['pyramid/skin_base'] = """
1층 껍데기. **구조물 블록으로는 저장할 수 없다** — 한 변 48칸이 한계인데 이건 91칸이다.
그래서 피라미드는 전부 코드가 만든다.

가운데 63×63은 `structure_void`다. 그 자리는 `core`가 채우므로 껍데기가 건드리면 안 된다.
북면에 **입구 터널이 새겨져 있고**, 터널 끝은 `core`의 북쪽 구멍과 좌표가 맞는다 — 직소가
아니라 표에서 온 숫자다.

직소 둘: 동쪽 `pyr_door`는 core를 자기 박스 안에 매달고(형태 울타리의 출발점), 위쪽은
`skin_mid`를 올린다.
"""

NOTES['pyramid/skin_mid'] = """
2~4층 껍데기. 가운데 구멍이 커서(`structure_void` 53200) 21칸 높이의 묘실이 통째로 들어간다.
아래 직소 `pyr_anchor`가 `skin_base` 위 직소에 걸리는 유일한 입구고, 이름이 하나뿐이라
뒤집혀 붙을 수 없다.
"""

NOTES['pyramid/skin_top'] = """
5층과 꼭대기. 여기부터는 속이 거의 비어 있고(void 14280), 석관실 하나를 품는다.
꼭대기 18칸은 통짜 사암 뿔이다.
"""

NOTES['pyramid/core'] = """
**63×63×7의 통짜 사암 덩어리.** 미로가 이걸 파내면서 생긴다. 조각이 놓이는 것이 곧 파내는
것이라 별도의 굴착이 없다.

이 조각이 **형태 보장의 전부**다. 모든 미로 조각은 여기서 뻗어 나온 자손이고, 바닐라는
자식을 부모의 박스 안에서만 판정하므로 무엇도 63×63 밖으로 못 나간다. 피라미드 가장자리까지
8블록이 남는다.

북면에 뚫린 3×4(x 30–32)가 입구 터널의 안쪽 끝이다.
"""

for _i, _next in ((1, 'spine_2'), (2, 'spine_3'), (3, 'well')):
    NOTES['pyramid/spine_%d' % _i] = """
입구에서 우물까지 이어지는 결정적 사슬의 %d번째 칸. 북문이 `pyr_spine` 입구(하나뿐),
남문이 다음 칸(`%s`)을 부른다. 서·동문은 평범한 `passages`라 스파인에서도 미로가 갈라진다.
**입구에서 중앙까지 최소 세 칸**을 강제하는 것이 목적이다.
""" % (_i, _next)

NOTES['pyramid/well'] = """
스파인의 끝이자 피라미드의 중심. **천장에 사다리 수직통로가 뚫려 있다** — 위쪽 8칸(x 2–4,
z 2–4)이 열려 있고 그 위는 `tomb`의 바닥 구멍이다. 두 조각 다 코드가 같은 표를 보고 파므로
직소 없이도 정확히 만난다.

서·동·남 세 문이 미로로 나가서, 미로는 사실상 이 방을 중심으로 퍼진다.
"""

NOTES['pyramid/tomb'] = """
21×21×21 묘실. **입구가 바닥 한가운데**라는 점이 지하감옥 보스방과 다르다 — 사다리를 타고
올라오면 이미 방 한복판이고, 사분면 스포너 넷과 단상의 파라오가 사방을 둘러싸고 있다.
도망칠 문이 없다.

천장 근처(x 14–16, z 17–19)에 해치가 있고 그 위가 석관실이다. 사다리 21칸이 그리로 올린다.
"""

NOTES['pyramid/crypt'] = """
묘실 천장 위 석관실. 문이 없고 **바닥 해치로만** 들어온다. 마법 황금사과가 든 상자 하나가
확정으로 있다. 보스를 잡은 사람만 닿는 자리라 전리품이 확정이어도 된다.
"""

NOTES['pyramid/passage'] = '미로의 기본 단위. 서-동 직선, 가중치 10.'

NOTES['pyramid/corner'] = """
서-남 모퉁이. 지하감옥에는 없는 조각이다. 피라미드 미로는 한 층뿐이라 평면이 단조로워지기
쉬워서 모퉁이를 가중치 8로 높게 넣었다.
"""

NOTES['pyramid/cross'] = '십자 교차로.'

NOTES['pyramid/niche'] = """
문 하나짜리 막다른 벽감. 상자 하나(`chests/pyramid_niche`). 지하감옥의 `dead_end`에 해당하지만
스포너가 없어서 더 가볍고, 가중치 9로 자주 나온다.
"""

NOTES['pyramid/sand_den'] = """
**모래가 되찾은 칸.** 통로는 뚫려 있지만 나머지는 모래로 막혀 있고, 알코브에 허스크 스포너가
묻혀 있다. 트리거가 없는 위험 — 파고 들어가는 것도, 지나치는 것도 플레이어의 선택이다.
"""

NOTES['pyramid/dig_site'] = """
모래 언덕에 수상한 모래가 묻혀 있다. **솔질은 몹이 할 수 없는 유일한 상호작용**이라 함정을
전부 뺀 뒤에도 이 조각만 남았다. 전리품은 `archaeology/pyramid_sand`.
"""

NOTES['pyramid/guard_post'] = """
교차로 한가운데 허스크 스포너. 지하감옥의 `cross_guard`와 같은 역할이다.
"""

NOTES['pyramid/cap'] = '두께 1의 사암 벽. 모든 풀의 fallback.'




# =========================================================================================
# The tower's prose lives in its own module: it is the longest of the three and the one that
# changes most often.
from level_notes_tower import install as _install_tower   # noqa: E402

_install_tower(INTRO, NOTES)

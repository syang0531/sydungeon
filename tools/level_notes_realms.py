# -*- coding: utf-8 -*-
"""The nether's three and the end's two. Imported by level_notes.py."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from generate_nether import SKINS  # noqa: E402

NETHER_PRINCIPLE = """
### 네더에는 지표가 없다

`WORLD_SURFACE_WG`는 네더에서 **기반암 천장까지** 잰다. 거기에 시작 조각을 투영하면 던전이
천장에 매달린다. 바닐라 잔해(bastion)가 하는 유일한 방법이 답이다 — **`start_height`를 범위로
주고 heightmap을 아예 쓰지 않는다.** 시작 조각이 y 38~78 어딘가의 바위에 떨어지고, 조각들이
자기 방을 파낸다. 네더락 속에 묻힌 요새란 원래 그런 것이다.

### 하나의 기계, 셋의 껍질

`tools/generate_nether.py`의 `SKINS` 표가 재질·바이옴·몹·보스·간격만 말한다 (§35와 같은 수법).
모양은 한 번만 쓰여 있다: 시작 홀 하나(21³), 복도 일곱 종, 마개, 보스 사슬 셋, 보스 홀
21×14×21.

### 기존 구조물을 비껴 선다

네더에는 이미 요새와 잔해가 있다. 바닐라가 그 둘을 `minecraft:nether_complexes` 한 세트에
묶어 둔 이유가 이것이고, 우리 셋은 전부 그 세트에 **`exclusion_zone` 8청크**를 건다
(`docs/concepts.md` §5의 요구).

### 용암은 다섯 면이 막혀 있다

§28. `span`의 수로도, `forge`의 홈도, 보스 홀의 도랑도 전부 **바닥 켜 한 칸 위**에 테두리
안쪽으로 놓인다. 첫 판은 보스 홀의 도랑을 조각 바닥면에 깔았고, 검사기가 잡았다 — 그대로
뒀으면 월드가 사는 동안 아래로 흘러내렸을 것이다.
"""

GHASTS = """
### 가스트는 여기 없다

콘셉트 §5.3은 보스전에 가스트를 원했다. 가스트의 화염구는 **블록을 부순다**. 부술 블록이
지금 싸우고 있는 홀이다 — 피라미드의 TNT와 같은 반대이고(§12), 방향만 다르다. 성소의 목소리는
위더 스켈레톤이고 가스트의 눈물은 상자에 있다.
"""


def nether_intro(skin, extra=''):
    biomes = ' · '.join(skin['biomes'])
    return {
        'title': '# 레벨 설계 — %s' % skin['title'],
        'summary': """
네더의 대규모 던전이다. 바위 속 홀 하나에서 복도가 사방으로 뻗고, 통로 셋 뒤에 **%s**가 있다.

지표가 없으므로 입구도 사다리도 없다 — y 38~78 어딘가에 떨어진 시작 홀이 곧 입구다.

바이옴: %s · spacing %d / separation %d
""" % (skin['boss'], biomes, skin['spacing'], skin['separation']),
        'principle': NETHER_PRINCIPLE + extra,
        'skeleton': """
시작 홀과 그 남문이 부르는 보스 사슬. 복도는 판마다 다르다.
""",
        'floors': [('복도', 1), ('홀 위쪽', 6)],
        'order': ['gate', 'passage', 'corner', 'cross', 'guard', 'span', 'store',
                  'special', 'cap', 'approach_1', 'hall'],
        'outro': """
## 다듬을 곳

- **복도가 일곱 종뿐이다.** 같은 문 배치의 조각을 더 만들어 풀에 넣으면 그만큼 다양해지고
  배선은 안 건드려도 된다 (§16)
- **떨어지는 높이가 무작위다.** 바닐라 잔해와 같은 방식이라 가끔 용암 바다 위나 큰 동굴
  한복판에 걸린다. `beard_thin`이 밑을 받쳐 주지만 12칸까지다 (§31)
- **손으로 지은 조각이 하나도 없다.** 21×21×21까지는 구조물 블록 한 변 48 안이므로
  §16대로 다시 지어 `tools/handmade/`에 넣을 수 있다
""",
    }


def install(INTRO, NOTES):
    for skin in SKINS:
        key = skin['key']
        INTRO[key] = nether_intro(skin, GHASTS if key == 'sanctum' else '')
        NOTES['%s/gate' % key] = """
시작 조각. 21×21×21 홀이고 열 칸 높이다. 서·동·북문이 복도로 가고 **남문이 보스 사슬**을
부른다 — 지표가 없으니 이 방이 곧 입구다.
"""
        NOTES['%s/passage' % key] = '복도의 기본 단위.'
        NOTES['%s/corner' % key] = '꺾이는 복도.'
        NOTES['%s/cross' % key] = '네거리. 네 구석에 기둥.'
        NOTES['%s/guard' % key] = '경비 칸. %s 스포너.' % skin['mob'].split(':')[-1]
        NOTES['%s/span' % key] = """
용암 수로 둘 사이의 좁은 길. 트리거가 없는 위험이고, 먼저 지나가는 것이 몹이든 사람이든
똑같다 (§12). 용암은 바닥 켜 한 칸 위, 벽돌 테두리 안쪽이다 (§28).
"""
        NOTES['%s/store' % key] = '상자 방. 상자와 통.'
        NOTES['%s/cap' % key] = '두께 1의 마개. 복도 풀의 fallback (§4).'
        NOTES['%s/approach_1' % key] = """
보스 통로. 셋이 사슬로 이어져 보스 홀을 시작 홀에서 멀리 민다 (§7). 서쪽 직소만
`%s_boss` 이름이라 사슬이 거꾸로 붙지 않는다 (§13).
""" % key
        NOTES['%s/hall' % key] = """
%s의 홀. 21×14×21. 기둥 넷, 단상 위 보스 트라이얼 스포너, 네 구석에 경비 스포너,
남쪽에 테두리를 두른 용암 도랑.
""" % skin['boss']

    NOTES['fort/special'] = '용광로. 북벽을 따라 용암 홈, 남벽에 용광로 둘과 상자.'
    NOTES['vault/special'] = '금고. 벽에 금 블록, 상자 하나, 그리고 그것을 지키는 피글린.'
    NOTES['sanctum/special'] = '납골 선반. 뼈 블록 두 줄과 가운데 영혼 불.'

    # ------------------------------------------------------------------------------ 엔드
    INTRO['spire'] = {
        'title': '# 레벨 설계 — 공허 첨탑',
        'summary': """
엔드 고지의 탑이다. 섬 위에 선 21×21 밑층에서 **위로** 올라간다 — 이 모드에서 유일하게
올라가는 던전이다. 층 넷을 지나 꼭대기 왕관 방에 셜커 여섯과 **엘리트라**가 있다.

시그니처(확정): **엘리트라 + 셜커 껍데기 2 + 마법 부여 다이아 1.** spacing 30 / separation 10.
""",
        'principle': """
### 올라가는 것은 내려가는 것과 같은 기둥이다

§33은 "구멍은 한 칸이고 둘레 여덟은 막혀 있다"인데, 그 규칙에 위아래가 없다. 첫탑은 같은
기둥을 반대로 읽는다 — 층마다 **위 직소**가 다음 층을 부르고, 사다리는 바닥 켜를 제 손으로
뚫는다. 층이 넷인 것은 보스 사슬과 같은 수법이다 (§7): `floor_1`이 `floor_2`를 부르고,
`floor_4`가 왕관을 부른다.

### 순서 함정, 또

왕관 방의 첫 판은 **직소를 먼저 쓰고 기둥을 나중에** 세웠다. 기둥이 같은 칸을 지나므로 직소를
지워 버렸고, 탑이 네 층에서 끝났다. §11이 피라미드에서 적어 둔 것과 같은 함정이다 — 검사기가
조립해서 올라가 보고 어느 높이에서 멈추는지 말해 준다.

### 엔드에서는 heightmap이 통한다

섬은 단단하고 위는 하늘이라 `WORLD_SURFACE_WG`가 제대로 잰다 (네더와 정반대다). 대신 섬
가장자리는 공허로 떨어지는 절벽이므로 `level_ground_drop` 12를 건다 (§34).
""",
        'skeleton': """
밑층, 층 넷, 왕관. 전부 단일 원소 풀이라 첫탑은 언제나 같은 높이다.
""",
        'floors': [('밑층', 2), ('둘째 층', 9), ('왕관', 33)],
        'order': ['base', 'floor_1', 'floor_2', 'floor_3', 'floor_4', 'crown'],
        'outro': """
## 다듬을 곳

- **층이 다 같이 생겼다.** 홀수 층에 상자, 짝수 층에 셜커 스포너뿐이다. 층마다 다른
  방을 손으로 지으면 마법사의 탑처럼 될 수 있다 (§16)
- **"진주로만 건너는 틈"이 없다.** 콘셉트의 기믹인데, 사다리를 끊으면 검사기가 막고
  플레이어는 버그로 읽는다. 넣으려면 끊긴 곳이 **끊긴 것처럼 보여야** 한다
""",
    }
    NOTES['spire/base'] = """
시작 조각. 섬 위에 서고 바닥 켜가 곧 섬의 맨 윗 블록이다 (§30). 서쪽에 문, 가운데에
올라가는 기둥.
"""
    for i in (1, 2, 3, 4):
        NOTES['spire/floor_%d' % i] = (
            '%d층. 가운데 사다리 기둥이 바닥을 뚫고 지나가고, %s.'
            % (i, '상자 하나' if i % 2 else '셜커 스포너 하나'))
    NOTES['spire/crown'] = """
왕관 방. 21×14×21. 단상에 **첨탑의 심장**(체력 50 셜커) 트라이얼 스포너, 네 기둥 발치에
경비 스포너 넷. 이기면 **엘리트라**가 나온다.
"""

    INTRO['chorus'] = {
        'title': '# 레벨 설계 — 코러스 미궁',
        'summary': """
엔드 중지의 미로다. **지붕이 없다** — 엔드 돌 벽돌 담이 다섯 칸 높이로 서 있고 그 사이를
걷는다. 코러스가 자란 칸과 엔더마이트 둥지를 지나 통로 셋 뒤에 **엔더맨 무리**가 있다.

시그니처(확정): **엔더 진주 16 + 흑요석 8 + 마법 부여 다이아 1.** spacing 36 / separation 12.
""",
        'principle': """
### 지붕이 없는 미로는 엔드에서만 된다

비도 눈도 없고 위에서 내려다볼 것도 없다. 그래서 칸은 바닥과 담뿐이고, 천장을 안 쓰는 만큼
조각이 가볍다. 지붕이 있는 방은 하나뿐이다 — 보스 홀.

### 엔더맨은 지붕 밑에서만 잡힌다

트라이얼 스포너는 **제가 낸 몹이 다 죽어야** 끝난다 (§26). 하늘이 보이는 엔더맨은 순간이동할
곳이 많아서 영영 안 죽을 수 있다. 그래서 보스 홀만 지붕을 덮었다.

### 나머지는 지금까지의 규칙

바닥 켜는 y=0 (§30), 보스는 단일 원소 풀 사슬에 걸리고 우선순위가 높다 (§7), 마개는 담 한
장이다 (§4). 바닐라 엔드 도시와는 `exclusion_zone` 8청크로 떨어뜨린다.
""",
        'skeleton': """
광장과 그 남문이 부르는 보스 사슬. 미로는 판마다 다르다.
""",
        'floors': [('미로', 1), ('담 위', 5)],
        'order': ['plaza', 'run', 'bend', 'fork', 'grove', 'nest', 'vault', 'cap',
                  'approach_1', 'hall'],
        'outro': """
## 다듬을 곳

- **담이 한 가지 무늬다.** 엔드 돌 벽돌에 퍼퍼가 섞인 것뿐이라 멀리서 보면 평평하다
- **코러스가 자라지 않는다.** 심은 그대로고, 자라게 두면 미로를 덮는다
- **텔레포트하는 벽이 없다.** 콘셉트의 기믹인데 Java가 필요하다 (§1)
""",
    }
    NOTES['chorus/plaza'] = """
시작 조각. 21×21의 열린 광장이고 담이 다섯 칸이다. 서·북·동문이 미로로, **남문이 보스
사슬**로 간다. 가운데 단 위에 상자, 구석에 엔더맨 스포너.
"""
    NOTES['chorus/run'] = '미로의 기본 단위. 지붕이 없다.'
    NOTES['chorus/bend'] = '꺾이는 길.'
    NOTES['chorus/fork'] = '네거리. 네 구석에 퍼퍼 기둥.'
    NOTES['chorus/grove'] = '코러스가 자란 칸. 엔드 돌 위에 줄기 셋과 꽃.'
    NOTES['chorus/nest'] = """
엔더마이트 둥지. 낮은 담 안에 스포너 하나. 처음엔 바닥을 판 구멍이었는데, 그 바닥이 조각의
맨 아랫면이라 밑에 받칠 것이 없었다.
"""
    NOTES['chorus/vault'] = '막다른 보물 칸. 상자 하나.'
    NOTES['chorus/cap'] = '담 한 장. 미로 풀의 fallback (§4).'
    NOTES['chorus/approach_1'] = '보스 통로. 셋이 사슬로 이어진다 (§7).'
    NOTES['chorus/hall'] = """
미로에서 **유일하게 지붕이 있는 방**. 21×14×21. 단상에 엔더맨 트라이얼 스포너, 네 구석에
경비. 이기면 **엔더 진주 16과 흑요석 8**이 나온다 — 엔더 상자 하나치다.
"""

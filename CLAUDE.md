# SY Dungeon

마인크래프트 자바 에디션 모드. **손으로 지은 조각을 직소가 이어 붙여, 매번 다른 던전을 만든다.**

**목적: 파밍과 채굴 없이, 탐험만으로 엔드 콘텐츠(엔더 드래곤·풀 마법 부여·네더라이트)에 닿는다.** **바이옴마다 시그니처 하나**, 그것을 갖는 대규모 던전 하나, 그리고 보스도 시그니처도 없는 중소규모 하나둘 (`docs/concepts.md` §2.7~2.8). 바닐라가 파밍·채굴로 주던 것을 던전 보상으로 옮긴다. 전체 설계는 `docs/concepts.md`, 던전별 레벨 설계(조각 단면도와 조립 원리)는 `docs/levels/`다. 지하감옥·대피라미드·마법사의 탑·마녀의 늪·얼음 성채·덩굴에 삼킨 신전·군벌의 진영·저주받은 묘지를 만들었고, 드워프의 전당·침몰한 등대를 만들었고, 중소규모로 균사 동굴·산신의 사당과 **한 기계에서 나오는 여덟**(우물·무덤·오두막·돌무덤·마른우물·저장고·지하실·늪저장고)을 만들었다. **어느 바이옴이 어느 던전 것인지는 `docs/concepts.md` §4.0 표가 유일한 출처다** — 태그 파일을 각자 고치면 두 던전이 같은 자리를 다툰다. 보상 테이블에 다른 모드의 아이템은 넣지 않는다.

## 환경

| 항목 | 값 |
|---|---|
| Minecraft | 26.2 (Java Edition) |
| 모드로더 | NeoForge 26.2.0.88 |
| Java | 25 |
| modid | `sydungeon` |
| 패키지 루트 | `com.syang.sydungeon` |
| 빌드 | `./gradlew build` / 실행 `./gradlew runClient` |

> **26.2로 고정한다.** 연습 조각은 26.3 월드에서 저장됐고, 26.2와 26.3 사이에 구조물 NBT 팔레트 키가 바뀌었다 (`Name`/`Properties` → `id`/`properties`, DataVersion 4903 → 5023). `tools/convert_yame.py`가 키를 되돌려 26.2 파일로 쓴다. 블록 자체는 둘 다 같은 것들이라 그것으로 끝이다. **새 조각은 26.2 월드에서 저장하는 것이 맞다** — 그러면 변환이 필요 없다. NeoForge 26.3은 2026-09-20 기준 베타뿐이다.

## 불변 원칙

### 1. 모양은 월드에서, 규칙은 JSON에서, 코드는 그 둘이 못 하는 것만

던전의 모양을 정하는 것은 `data/sydungeon/structure/**/*.nbt`(크리에이티브에서 구조물 블록으로 저장)이고, 어떻게 이어지는지는 `data/sydungeon/worldgen/`의 template_pool · structure · structure_set · processor_list다. **Java는 바닐라 직소가 못 하는 일이 생길 때만 쓴다.** 지금까지 그런 일은 하나다: 바닐라 `minecraft:jigsaw`의 `size`는 정수 하나라 던전 깊이가 늘 같다. `sydungeon:ranged_jigsaw`(`worldgen/RangedJigsawStructure`)는 그 복사본에 `size`를 IntProvider로 바꾼 것이다. 그 외 Java는 없다. 조각을 바꾸고 싶으면 코드가 아니라 월드에서 바꾸고, `tools/dump_structure.py`로 확인한다.

**예외 하나:** 전리품 상자와 스포너는 `tools/decorate.py`가 NBT로 넣는다. 구조물 블록은 상자의 *아이템*을 저장하고 loot table을 저장하지 못하며, 스포너는 `/setblock`에 친 것이 그대로 들어가서 읽을 수 없다. 두 블록엔티티의 NBT는 한 파일에서 읽히는 편이 낫다.

규칙으로 짓는 것은 처음엔 빠르지만 고칠 때마다 코드를 고쳐야 한다. 템플릿은 월드에서 고친다.

### 2. 조각은 7×7×7 격자 셀이다

- 조각의 크기는 7의 배수. 계단은 7×14×7로 두 층을 차지한다
- 출입구는 면 중앙에 **3폭 × 4높이** (x 또는 z = 2..4, y = 1..4)
- 직소는 **바닥층(y=0), 그 면의 가운데**. `final_state`는 `minecraft:stone_bricks`
- 공기를 **전부 저장**한다(7×7×7 = 343블록). `rigid`로 놓이면 지형을 파낸다
- **`structure_void`는 쓰지 않는다.** 템플릿은 목록에 있는 블록만 놓으므로 "손대지 마라"는 뜻은 **목록에서 빼는 것**이다(`Piece.write`가 뺀다). 넣으면 진짜 블록으로 박히는데, 안 보이고 통과되는 블록이라 둥근 탑 발치의 잔디를 덮어써서 그 아래 흙이 드러났다

격자가 정확해야 두 가지가 같은 셀로 수렴해도 충돌 판정이 깔끔하고, 뚫린 문이 이웃 조각의 돌벽에 막혀 "막아둔 문"처럼 보인다.

### 3. 커넥터 이름은 둘, 무엇이 붙을지는 풀이 정한다

미로의 직소는 전부 name = target = `sydungeon:door`. 어떤 조각이 붙을 수 있는지는 직소가 가리키는 **풀**(`passages` / `cells`)이 정한다. 이름을 나누면 얻는 것은 없고 caps 풀에 마개가 하나 더 필요해진다.

예외가 보스 가지다. 바닐라는 자식 조각의 **아무 직소나** 이름이 맞으면 입구로 쓴다. 십자형 `boss_passage`를 `door` 하나로 배선하면 절반은 "다음 보스 통로" 문으로 들어와 버려서 가지가 방을 잃는다. 그래서 보스 조각의 입구만 `sydungeon:boss_door`이고, 보스 가지를 잇는 직소는 target이 `boss_door`다. 그 문으로만 들어올 수 있다.

### 4. 모든 풀의 fallback은 `caps`다

직소 생성은 최대 깊이에 닿거나 옆 조각과 충돌하면 fallback 풀에서 고른다. fallback이 `minecraft:empty`면 출입구가 흙·돌·동굴로 뚫린 채 남는다. 바닐라 마을이 `terminators`를 fallback으로 두는 이유가 이것이다.

마개(`cap`)는 **1×7×7 벽 한 장**이다. 두께가 1이라 최대 깊이에서도 거의 항상 들어간다. 7칸 깊이의 막다른 방(`dead_end`)은 본 풀에 낮은 가중치로만 둔다 — fallback으로 쓰면 그것도 충돌해서 구멍이 남는다.

### 5. 시작은 지표, 미로는 그 아래

시작 조각 `entrance`는 `project_start_to_heightmap`으로 지표에 선다. 바닥 구멍의 아래 직소가 `shaft_first`(=`shaft_1`)를 부르고, `shaft_1`이 `shaft_second`(=`shaft_2`)를 부른 뒤에야 `shafts` 풀(shaft 3 : hub 2)이 이어지다가 `hub`(사다리가 닿는 방, 문 셋)에서 미로(`passages`)가 시작된다. **수직통로 둘이 강제되므로 허브는 문에서 최소 42칸 아래다.**

### 5.1 미로는 위로 자라지 않는다

땅 위로 솟아 나오는 것을 막는 방법은 두 개뿐이고, 둘 다 써야 한다.

1. **깊이 시작한다.** 하나만 강제했을 때는 허브가 21칸 아래였고, 던전이 80칸쯤 퍼지므로 그 사이에 21칸 내려가는 **평범한 언덕**이면 통로가 밖으로 나왔다. 42칸은 절벽이라 드물다
2. **올라가지 못하게 한다.** `stair`의 두 직소는 둘 다 서쪽 면인데 위만 `door`고 **아래는 `stair_foot`**이라 미로가 붙을 수 없다. 계단은 위에서만 들어오고, 그래서 미로는 허브보다 높아질 수 없다. 둘 다 `door`였을 때 미로는 ±7의 랜덤워크였고, 계단 서른 개면 표준편차가 38칸이라 **허브를 42칸 내려도 지표를 뚫었다**

### 5.2 지하 조각의 벽은 지형의 돌이다

깊이로도 못 막는 경우가 있다. 악지는 한 구조물 안에서 지형이 30칸 무너지고, 산은 그보다 더
떨어진다. 그래서 두 가지를 같이 한다.

1. **지형의 돌로 짓는다.** 군벌의 진영 갱도는 벽과 천장이 `stone`이고 다섯 켜마다
   `orange_terracotta` 띠가 들어간다. 노출돼도 **지층 한 겹**으로 보인다 — 굳은 진흙으로 만든
   납작한 상자가 침식 사면에 박혀 있으면 실수로 보인다(2026-09-22 스크린샷). 안쪽 치장(흙
   바닥, 울타리 기둥, 판자 천장)은 그대로라 들어가 보면 갱도다
2. **퍼지는 범위를 줄인다.** `max_distance_from_center.horizontal`을 80 → 56으로. 미로가
   고원 아래를 벗어나 사면까지 기어나가지 못한다

수직통로 길이도 지형에 맞춘다: 진영 35칸(악지), 얼음 성채 42칸(산), 지하감옥 42칸(§5.1).

`terrain_adaptation`으로는 못 막는다. `bury`·`encapsulate`는 조각 주변을 흙으로 채워 주지만 **구조물 전체에 한 값만 줄 수 있어서**(26.2 `Beardifier`는 `start.getStructure().terrainAdaptation()`을 조각마다 그대로 쓴다) 지표의 입구 정자까지 12칸 흙에 묻힌다. 내려가는 깊이는 `max_distance_from_center.vertical`과 `dimension_padding.bottom`이 막아 주므로 세계 밑으로는 못 간다.

같은 이유로 얼음 성채의 수직통로도 28칸이다 — 산 바이옴이라 더 급하다. **사다리는 조각 정중앙 한 칸(x=3, z=3)이고 기둥은 그 북쪽 한 칸(z=2)이다** (§33). 직소도 그 기둥에 선다. 수직 직소는 전부 `joint=aligned`라 기둥이 내려가는 내내 같은 쪽에 있다.

직소 깊이는 경로마다 센다. 수직통로 사슬이 2~3을 쓰고, 미로 가지는 각자 나머지를 쓴다. `size`가 13~23인 이유가 그것이다: 미로 깊이 10~20에 수직통로 값을 더한 것.

### 30. 시작 조각의 바닥 켜는 y=0이다

`JigsawPlacement`는 시작 조각을 **`minY + getGroundLevelDelta()`가 heightmap에 오도록** 옮기고,
`StructurePoolElement.getGroundLevelDelta()`는 **1**이다 (26.2에서 확인). heightmap은 **첫 빈
칸**이므로 옮기고 나면 이렇게 된다:

```
y = 0   지형의 맨 윗 블록 — 우리 바닥 켜가 놓일 자리
y = 1   그 위 첫 공기 — 플레이어가 서는 칸
```

그래서 **시작 조각에 기초를 달면 땅속으로 들어가지 않고 잔디 위로 솟는다.** 문간이 그만큼
같이 올라가 땅에 안 닿고, `beard_thin`이 그 높이를 맞추려고 **열두 칸 밖까지 돌을 채워**
인공적인 회색 광장을 만든다. 0.11.0에서 여섯 던전이 그랬다(2026-09-22 스샷) — 문간 7,
움집 7, 예배당 5, 사당 5, 굴 입구 4, 등대 3칸씩.

**기초는 자식 조각의 것이다.** 자식은 제 직소의 높이로만 놓이므로 밑이 꺼진 자리에서는
정말로 공중에 뜬다 (§18). 시작 조각은 반대로 지형에 붙어 있고, 꺼진 곳은 beard가 받친다.

예외는 마녀의 늪 하나다. 덱이 물 위 세 켜에 있고 말뚝이 거기서 내려가야 하므로 일부러
띄운다 (§17). `tools/check_starts.py`가 그 하나만 예외로 두고 나머지를 전부 검사하며,
`validate_data.py`가 빌드 전에 그것을 부른다.

### 31. beard는 조각 바닥 **위를 깎고** 아래를 채운다

`Beardifier`를 읽었다 (26.2). `BEARD_THIN`은 조각마다 `groundY = box.minY + 1`을 잡고,
**그 위의 지형은 지우고 그 아래는 채운다.** 커널 반경은 12칸이고 가우시안이라 실효 범위는
6칸쯤이다.

그래서 구조물 옆의 언덕은 조각 바닥 높이까지 **깎여 나가고**, 꺼진 곳은 돌로 **메워진다**.
그게 스샷의 "인위적으로 부서진 땅"이고, 해변에서는 그 깎기가 **해수면 아래까지 파서 바닷물이
흘러들어온다**(2026-09-22).

- `none` — 아무것도 안 한다. 조각은 제 발밑 블록을 덮어쓰고, 경사면에서는 한쪽이 묻히거나
  한 칸쯤 뜬다. **지상 조각이 작고 지형이 평평하면 이쪽이 낫다** — 지하감옥(7×7 정자,
  평원·숲), 균사 동굴(7×7, 버섯섬), 침몰한 등대(7×7, 해변)가 `none`이다
- `beard_thin` — 큰 지상 조각은 이것이 있어야 선다. 성채·진영·묘지·사당·전당·피라미드·탑·신전
- `bury`/`encapsulate`는 **채우기만** 한다(`getBuryContribution`은 항상 양수). 구조물 전체에
  한 값뿐이라 지표의 입구까지 흙에 묻힌다 (§5.2)

### 32. 기초를 땅속에 넣고 싶으면 `start_height`를 내린다

시작 조각은 제 바닥 아래로 못 내려간다 (§30). 그런데 `JigsawPlacement`는
`bottomY = start_height + heightmap`으로 계산하므로, **`start_height`가 그대로 오프셋이다.**

정글 신전이 그것을 쓴다. 신전은 기초 일곱 켜 안에 물살 수로를 품고 있어서(§23) 그만큼
**전체가 떠 있었다** — 문턱이 정글 바닥보다 일곱 칸 위였다(2026-09-22 스샷).
`start_height: {"absolute": -7}` 하나로 기초가 땅속으로 들어가고 입구와 대계단이 정글
바닥에서 시작한다. `check_starts.py`가 그 오프셋을 읽어서 기대값을 `1 - start_height`로
잡는다.

### 33. 내려가는 통로는 **한 칸**이고, 그 둘레 여덟 칸은 막혀 있다

3×3 구멍은 겪어 보면 계속 불편하다. **들어서면 사다리를 놓치고 떨어진다.** 그래서 층마다
"사다리가 있는 줄"을 발판으로 남겨야 했고(§24가 그것이었다), 그 줄이 또 구멍의 모양을
어지럽혔다.

**한 칸이면 구멍이 곧 사다리다.** 걸어 들어가는 것이 곧 타는 것이라 발판이 필요 없다.
자리는 **조각의 정중앙**이고, 붙잡을 벽은 바로 옆 한 칸(`JIG`)이 맡는다 — 직소도 그 기둥에
서므로 기둥은 어차피 막혀 있어야 한다.

**둘레 여덟 칸은 무엇이 있었든 덮어쓴다.** 전에는 "공기인 자리만" 메웠고, 그래서 가끔
구멍이 남아 물이 새어 들어왔다(2026-09-22). 이제 `sink()`가 3×3을 통째로 쓴 다음 가운데만
사다리로 바꾼다.

**막힘은 도착 방 바로 위까지다.** 방 안에서까지 둘레가 막혀 있으면 사다리에서 내릴 수가
없다. 그래서 허브 안에서는 기둥 하나만 남기고, `ladder_problems()`도 천장 켜부터 위로만
둘레를 검사한다.

열 던전이 전부 같은 모양이다 — 감옥·피라미드·늪·성채·신전·진영·묘지·굴·전당·등대.
피라미드와 신전처럼 방과 방을 잇는 짧은 사다리도 같다: 가운데 한 칸, 옆에 기둥.

**검사는 표를 믿지 않는다.** 던전마다 있는 `ladder_problems()`는 "조각이 이렇게 놓일
것이다"라는 손으로 쓴 오프셋 표와 대조한다. 늪의 `pilings`가 옛 3×3 시절 직소 좌표를 그대로
갖고 있었을 때, **조각마다의 사다리도 완벽했고 표도 자기끼리 맞았는데** 게임에서는 그 아래
전체가 한 칸 어긋나 걸렸다 — 사다리가 있어야 할 자리에 판자가 있고, 한 칸 옆에서 다시
사다리가 시작했다. `tools/check_shafts.py`는 그래서 **위치를 말하지 않는다**: 풀을 읽고,
아래쪽 직소가 부르는 단일 원소 조각을 찾고, 그 조각에서 맞받는 위쪽 직소를 찾아 빼기로
자리를 정한다. 게임이 하는 일이 그것뿐이다.

### 34. 구조물이 절벽 끝에 서지 않게 하려면 **Java가 땅을 봐야 한다**

직소 구조물은 지형을 보지 않는다. `beard_thin`도 12칸까지만 닿으므로, 그보다 깊은 절벽
가장자리에 49칸짜리 성채가 앉으면 기초가 허공에 걸린 채로 선다(2026-09-22 스샷).
데이터로는 막을 방법이 없다 — 그래서 §1의 예외가 하나 늘었다.

`RangedJigsawStructure`에 **`level_ground_drop`**(허용 낙차)과 **`level_ground_radius`**를
달았다. `findGenerationPoint`가 후보 자리마다 heightmap을 **아홉 점**(가운데·네 모서리·네 변)
찍어서 최고−최저가 허용치를 넘으면 그 자리를 포기한다. 둘 다 선택 항목이라 안 쓰는 던전은
전과 똑같다.

**값을 후하게 잡는다.** 거절한 자리는 없어진 던전이다. 지금은 성채·진영 14/24, 신전 16/28,
탑 12/14, 묘지 14/10, 전당 16/10, 사당 12/10이다. 피라미드는 안 쓴다 — 사막은 평평하고
밑판이 두꺼워서 스스로 메운다.

### 6. 던전은 어둡다. 스포너는 빛을 무시한다

랜턴은 `entrance`와 `boss_room`에만 있다. `decorate.py`가 나머지 조각의 랜턴을 전부 뺀다. 어두운 통로에서는 바닐라 자연 스폰이 일어나고, 그게 긴장감의 대부분이다(2026-09-20, 밝아서 긴장감이 없다는 피드백). 원래 조각마다 랜턴이 있었고, 몬스터 스포너는 스폰되는 몹의 규칙을 따르므로 밝은 통로에서는 아무것도 내지 않는다. `decorate.py`의 SpawnData에 `custom_spawn_rules.block_light_limit 0..15`가 들어 있는 이유다. 빼면 스포너는 장식이 된다. 트라이얼 스포너는 빛을 보지 않는다.

### 7. 보스는 던전마다 하나, 미로보다 먼저 놓인다

hub의 남문이 `boss_approach_1`을 부르고, 그 풀의 유일한 원소 `boss_passage_1`의 동문이 `boss_approach_2`를 부르는 식으로 `boss_approach_5`까지 이어진 뒤에야 `boss_approach`(boss_passage 2 : boss_room 1)가 온다. 직소의 풀은 조각에 박히므로 "최소 5개"를 강제하려면 같은 통로의 복사본이 5개 필요하다(`generate_pieces.boss_chain`이 조각과 풀 JSON을 함께 쓴다). 방은 hub에서 통로 5~8개 뒤, 직선으로 35칸 이상이다. `boss_room`은 21×21×21, 즉 3×3×3 셀이라 미로가 먼저 자라면 충돌해서 못 놓인다. 그래서 보스 가지의 직소는 `placement_priority`·`selection_priority` 10이고(나머지는 0), 바닐라는 우선순위가 높은 조각부터 놓는다. boss_passage의 북·남문은 `passages`를 가리키므로 보스 가지는 미로를 줄이지 않는다.

**클리어 판정과 보상은 트라이얼 스포너가 한다.** 상자는 싸우지 않고 열 수 있고, "다 죽였는지"를 판정하려면 Java가 필요하다. 트라이얼 스포너는 인원에 맞춰 웨이브를 내고 다 잡으면 `loot_tables_to_eject`를 싸운 플레이어마다 뱉는다. 스포너 설정은 `data/sydungeon/trial_spawner/dungeon/`(boss, guards), 블록엔티티 NBT는 그 id만 가리킨다. `target_cooldown_length`가 사실상 무한이라 던전당 한 번이다.

### 8. 전리품 상자의 첫 풀은 생존 물자다

두 상자 테이블 모두 **풀이 둘**이다. 첫 풀은 rolls 1에 횃불·식량·석탄·**침대**만 들어 있어 상자마다 생존 물자 하나가 확정으로 나오고, 둘째 풀이 나머지를 굴린다. 던전이 어두워진 뒤로 횃불은 소모품이 아니라 필수품이고(§6), 파밍 없는 진행에서는 침대도 마찬가지다. 하나의 큰 풀에 섞으면 빵 네 개가 든 상자가 생긴다. 감방 상자 기준 침대 12%·횃불 24%.

### 9. 데이터의 id는 게임 jar와 대조한다

`python tools/validate_data.py`. 같은 종류의 버그가 두 번 났고 둘 다 빌드는 통과했다. `minecraft:chain`은 26.2에서 `iron_chain`이라 **감방 전리품 테이블 전체가 로드되지 않아 상자가 전부 비었고**, `minecraft:dappled_forest`는 26.3 전용이라 **월드가 아예 안 열렸다.** 검사기는 아이템·블록·엔티티·속성·바이옴·마법을 게임 jar에서 읽어 대조하고, 조각·풀·전리품·스포너 설정의 상호 참조와 아무도 안 쓰는 파일까지 본다. 빌드 전에 돌린다.

## 지하감옥 조각 (`structure/dungeon/`)

| 조각 | 크기 | 직소 | 풀 | 비고 |
|---|---|---|---|---|
| `entrance` | 7×7×7 | 아래(shaft_first) | start | 지표. 지붕 있는 정자, 바닥에 사다리 구멍. 생성 |
| `shaft` | 7×21×7 | 위·아래(shafts) | shafts(3) | 사다리 세 칸. 생성 |
| `hub` | 7×7×7 | 위(shafts) + 서·동(passages) + 남(boss_approach_1, 우선순위 10) | shafts(2) | 사다리가 닿는 방. 북벽에 사다리, 북문 없음. 생성 |
| `boss_passage_1..5` | 7×7×7 | 서(`boss_door` 입구)·동(→boss_approach_k+1, 우선순위 10)·북·남(passages) | boss_approach_k(1) | cross에서 파생. 최소 길이 강제용 |
| `boss_passage` | 7×7×7 | 위와 같되 동문이 boss_approach | boss_approach(2) | cross에서 파생 |
| `boss_room` | 21×21×21 | 서(`boss_door` 입구, 면 정중앙) | boss_approach(1) | 단상 위 보스 트라이얼 스포너 1, 사분면마다 경비 스포너 4, 모서리 기둥, 벽 띠, 쇠사슬 랜턴 8. 생성 |
| `cross_guard` | 7×7×7 | 서·동·북·남 | passages(4) | cross 가운데에 몬스터 스포너 |
| `shaft_cap` | 7×1×7 | 위 | shaft_caps | 수직통로 바닥 마개. 생성 |
| `passage` | 7×7×7 | 서·동 | passages | 직선 통로 |
| `passage_cell` | 7×7×7 | 서·동 + 남(cells) | passages | 남쪽 벽이 철창, 그 뒤에 감방 |
| `passage_cells` | 7×7×7 | 서·동 + 북·남(cells) | passages | 양쪽 철창 |
| `cross` | 7×7×7 | 서·동·북·남 | passages | 십자 교차로 |
| `stair` | 7×14×7 | 서(y=0)·서(y=7) | passages(가중치 5) | 한 층 위로. 층 간격 7 유지. 어느 문으로 붙느냐가 무작위라 절반은 아래층으로 이어진다 |
| `dead_end` | 7×7×7 | 서 | passages(가중치 5) | 경비실. 가운데 스포너(좀비·스켈레톤·거미·동굴거미), 구석에 상자 `chests/dungeon_guard` |
| `cell` | 7×7×7 | 북 | cells | 북쪽 면 전체 개방 → 부모의 철창에 붙음. **문이 없다**: 철창을 깨야 들어간다. 안쪽 벽에 상자 `chests/dungeon_cell` |
| `cap` | 1×7×7 | 서 | caps | 벽 마개. 생성 |

"생성"은 `tools/generate_pieces.py`가 코드로 만든 조각이다. 출발점일 뿐이니 개발 클라이언트에서 열어 예쁘게 고치고 같은 이름으로 저장해 가져오면 된다. 살아남아야 하는 것은 직소의 위치·이름·풀이고, `import_piece.py`가 그걸 검사한다. 연습 조각 `st_gate`는 쓰지 않는다(입구가 그 자리를 대신한다).

풍화는 `processor_list/dungeon_weathering.json`이 모든 풀 원소에 걸려 있다: 돌벽돌의 18%가 금간 벽돌, 14%가 이끼 벽돌, 4%가 조약돌, 철창의 8%가 사라진다. 조각은 그대로고 매번 다르게 보인다.

생성 위치는 **온대 숲과 평원 7종**(`tags/worldgen/biome/has_structure/dungeon.json`), spacing 30 / separation 10. 바이옴은 던전마다 겹치지 않게 나눈다 — 어두운숲·창백한 정원은 저주받은 묘지, 눈 타이가·그로브는 얼음 성채, 타이가 셋은 마법사의 탑, **바람 언덕 셋은 드워프의 전당** 몫이다 (`docs/concepts.md` §4.0 표가 유일한 출처다).

## 대피라미드 (`structure/pyramid/`)

0.2.0. 사막 전용, spacing 48 / separation 20. 밑변 91×91, 높이 46의 45° 피라미드다. 전부 `tools/generate_pyramid.py`가 코드로 만든다 — 구조물 블록은 한 변 48칸이 한계라 91칸짜리 껍데기는 **게임에서 저장할 수가 없다.**

### 10. 형태는 상자가 지킨다

직소 생성은 실루엣을 모른다. 그냥 두면 미로가 경사면을 뚫고 나간다. 보장은 `JigsawPlacement`의 규칙 하나에서 온다: **직소가 자기 조각의 바운딩 박스 안쪽을 가리키면, 그 자식들은 전역 공간이 아니라 그 박스를 여유 공간으로 삼아 판정된다.** 요새(bastion)가 큰 방을 작은 방으로 채우는 방법이고, 우리는 그걸 울타리로 쓴다.

```
skin_base (91×7×91)  ─안쪽→ core (63×7×63) ─안쪽→ 스파인, 그다음 미로
skin_mid  (77×21×77) ─안쪽→ tomb (21×21×21)
skin_top  (35×18×35) ─안쪽→ crypt (7×7×7)
```

미로 조각은 전부 `core`의 자손이므로 전부 core의 박스로 판정되고, **아무것도 core를 벗어날 수 없다.** core는 피라미드 가장자리에서 한 셀 이상(실측 8블록) 안쪽이라 경사면은 건드려지지 않는다. 차단 조각도, 관리할 void도 필요 없다 — 박스가 울타리다.

껍데기 셋은 `joint=aligned` 수직 직소 하나짜리 사슬로 쌓이므로 피라미드는 항상 온전하다.

**울타리는 자손에게 상속된다.** `attachInsideSource`가 참이면 자식의 `PieceState`가 부모의
VoxelShape를 **그대로 물려받고**, 그 자식이 다시 자식을 놓을 때도 그것을 쓴다(26.2
`JigsawPlacement.tryPlacingChildren`). 한 번 상자 안에 들어가면 **밖으로 나가는 길이 없다** —
상자 밖을 가리키는 직소를 달아도 자식이 거부될 뿐이다. 피라미드는 그걸 노린 것이지만,
"성벽 안에 서서 지하로 내려가는" 구조는 그래서 **만들 수 없다.** 가두고 싶지 않은 가지가
있으면 그 가지는 상자 안 조각의 자손이면 안 된다 (얼음 성채가 성벽을 여러 조각으로 나눈 이유).

### 11. 결정적 기하는 직소가 필요 없다

입구 터널, 우물에서 묘실로 오르는 사다리 수직통로, 묘실에서 지하실로 가는 해치 — 전부 **두 조각에 같은 좌표로 새겨** 놓는다. 앵커 직소 하나로 이어진 두 조각은 항상 같은 상대 위치에 놓이기 때문이다. `AT`/`SIZE` 표가 유일한 출처이고, `generate_pyramid.verify()`가 매번 검사한다: 박스 중첩, 경사면까지의 여유, 수직통로 양쪽이 만나는지, 입구가 이어지는지.

**순서 함정:** 천장 구멍을 사다리보다 **나중에** 파면 사다리 맨 윗칸이 지워진다. 한 번 그랬다.

### 조각

| 조각 | 크기 | 역할 |
|---|---|---|
| `skin_base` | 91×7×91 | 1층 껍데기. 북면에 입구 터널이 새겨져 있다 |
| `skin_mid` | 77×21×77 | 2~4층 껍데기. 묘실을 품는다 |
| `skin_top` | 35×18×35 | 5층과 꼭대기. 지하실을 품는다 |
| `core` | 63×7×63 | 9×9 미로 칸. 통짜 사암이고 미로가 파낸다 |
| `spine_1..3`, `well` | 7×7×7 | 입구에서 중앙까지 결정적 사슬. 우물 천장에 수직통로 |
| `tomb` | 21×21×21 | 묘실. 단상의 파라오 + 사분면 경비, 트라이얼 스포너 5 |
| `crypt` | 7×7×7 | 묘실 천장 위 석관실. 마법 황금사과 상자 |
| `passage` `corner` `cross` `niche` | 7×7×7 | 미로 |
| `sand_den` `dig_site` `guard_post` | 7×7×7 | 모래·발굴·스포너 (§12) |
| `cap` | 1×7×7 | 마개 |

### 12. 밟아서 터지는 함정은 몹이 먼저 밟는다

압력판도 철사 트랩도 플레이어를 가리지 않는다. 던전에는 스포너와 자연 스폰으로 몹이 돌아다니므로, **플레이어가 도착하기 전에 몹이 전부 발동시킨다.** 0.2.0의 첫 판에서 TNT 함정방은 플레이어가 들어오기도 전에 터져서 **피라미드 일부를 같이 날려버렸고**, 모래 천장 함정은 (다른 이유로 작동하지 않았지만) 작동했다면 같은 일이 났을 것이다.

그래서 트리거 함정을 전부 뺐다. 남은 것은 **트리거가 필요 없는 위험**이다.

- **`sand_den`** — 모래가 되찾은 칸. 통로는 열려 있지만 나머지는 모래로 막혀 있고 알코브에 허스크 스포너
- **`dig_site`** — 모래 언덕에 묻힌 수상한 모래. **솔질은 플레이어만 할 수 있다** — 몹이 발동시킬 수 없는 유일한 상호작용이고, 그래서 여기 남았다
- **`guard_post`** — 교차로 한가운데 허스크 스포너

> 모래 천장 함정이 작동하지 않은 이유도 기록해 둔다. 철사(y=1) 바로 위가 공기(y=2)였고 모래는 y=3부터였다. 철사의 상태 변화는 **인접 블록만** 갱신하므로 공기가 갱신되고 모래는 그대로 떠 있었다. 문 높이가 4칸이고 플레이어가 2칸을 쓰니, 모래를 철사 바로 위에 두면 통로가 막힌다. 이 칸 크기에서는 만들 수 없는 함정이다.

### 보상

파라오(허스크, 체력 80, 금 장비, 날카로움 III·화염 II 금검) + 경비 웨이브. 클리어하면 **흑요석 14 + 화염과 강철 + 마법 부여(22~30레벨) 다이아 장비 1개**가 확정이다. 앞의 둘은 네더 포탈과 마법 부여대고, 다이아 장비는 §2.4의 "보스당 한 조각"이다 (`docs/concepts.md`). 처음엔 다이아 장비를 빼먹어서 감옥 보스와 규칙이 어긋나 있었다. 그 위에 금·경험치·다이아·30레벨 책·사구 장식 템플릿. 지하실 상자에는 마법 황금사과가 확정.

## 마법사의 탑 (`structure/tower/`)

0.5.0. 타이가 셋에만, spacing 40 / separation 14. **밑변 29의 둥근 탑, 높이 56, 여덟 층.**
층별 단면도는 `docs/levels/tower.md`.

**이 탑은 손으로 지은 것이다.** 사용자가 개발 클라이언트에 층 배치도를 그렸고
(3×3 칸, 조각 사이 1칸 간격, 8층), `tools/scan_world.py`가 월드에서 읽고
`tools/grab_cells.py`가 칸으로 잘랐다. 코드가 한 일은 배선과 테두리 쌓기다.

```
평면      21×21 방 덩어리의 네 꼭짓점이 지름 29 원 위에 놓이고, 그 사이를 4칸 테두리가 메운다
          → 동서남북은 4칸 두껍고 대각선은 0. 진짜 원이다
shell     29×56×29  테두리 8층 ─안쪽→ core 21×56×21 (통짜 돌)
한 층      7×7 방 3×3 = 9칸.  8층 = 72칸
```

0.3.0은 39×39 직육면체에 사다리 하나, 0.4.0은 그걸 가늘게 만든 것이었다. 둘 다 코드가
지었고 둘 다 "외형/인테리어가 아쉽다"는 평을 받았다. 0.5.0은 사람이 지었다.

### 13. 결정적으로 놓는 조각은 부모가 찾는 이름의 직소를 **하나만** 가진다

바닐라는 자식의 **아무 직소나** 이름이 맞으면 입구로 쓰고 회전 넷을 다 시도한다. 마법사의
성에서 한 조각에 같은 이름 직소를 여럿 달았다가 절반이 뒤집혀 붙어 **보스방에 들어가는 길이
아예 없었다.** 지금 탑의 방 조각은 직소가 **정확히 하나**뿐이고 서쪽 면에 있다.

그래서 **회전이 공짜다.** 바닐라가 붙는 쪽에 맞춰 돌려 주므로 왼쪽으로 꺾는 길 하나가 네
방향 전부다. 목업에 그려진 네 개의 ㄱ자는 한 조각이었다. 좌우 반전만은 안 해 주므로
`corner_right`는 거울로 만든다.

### 14. 칸이 반드시 생겨야 하면 미로를 자라게 두지 않는다

직소 생성은 문에서 문으로 자라므로 **어떤 칸이 생긴다는 보장이 없다.** 이웃이 그쪽으로 문을
내야 생기고, 모서리가 잘 남는다. 지하감옥·피라미드는 안 생긴 칸이 그냥 돌이라 상관없었지만
(§10) 이 탑은 칸 하나가 곧 방이라 빈 칸이 구멍이다.

그래서 **`core`가 칸마다 직소를 하나씩 갖고 72칸을 전부 직접 놓는다.** 무작위인 것은 어느
조각이 들어오느냐지 칸이 생기느냐가 아니다.

이어지는 것은 따로 보장한다. 층마다 **경로**(도착 칸 → 다음 계단 칸)가 있고, 경로 칸은
그 방향으로 문이 있는 것이 보장된 풀에서 뽑는다. 풀은 방향이 아니라 **들어온 문 기준 나머지
문이 어디 있나**로 나뉜다 — `link_through` / `link_left` / `link_right` / `link_cross` / `leaf`.

층 사이는 계단이 잇는다. 계단(7×14×7)은 위아래가 **둘 다 서쪽으로 열려서** 들어간 칸 바로
위로 나온다. 그래서 (도착 칸, 아래 계단) 둘만 층 사이로 넘어가고, 배치 전체가
`tools/tower_layout.py`의 작은 동적 계획이 된다. **계단과 2×2 방은 같은 칸을 못 쓴다** —
처음 짠 배치가 4층 계단을 도서관 안에 넣었다.

### 15. 경로가 층을 다 덮으면 안 된다

경로를 최대로 뽑으면 49칸이 경로에 들고 곁방이 하나 남았다. 그런데 **통로는 방이 될 수
없다** — 문이 최소 둘이어야 하니까. 그러면 보상 방도 비밀방도 설 자리가 없고 탑 전체가
복도가 된다.

경로는 **한 층의 60% 정도**(`ROUTE_SHARE`)만 덮고 나머지는 곁방이다. 대신 **곁방은 반드시
경로에 닿아야 한다**는 조건이 걸려서 이웃이 그쪽으로 문을 연다.
72칸 = 경로 33 + 곁방 17 + 계단 14(7개×2층) + 큰 방 8.

**비밀방은 문이 없다.** 문 없는 조각은 직소가 놓을 수 없으므로 직소를 **벽 속에** 박았다.
이웃의 문이 그 벽에 붙어 **돌로 막힌 문**이 되고, 층이 한 칸 모자란 것을 눈치채고 파야
상자가 나온다. `verify()`가 곁방을 전부 비밀방으로 바꿔도 꼭대기에 닿는지 따로 확인한다.

### 조각

| 조각 | 크기 | 역할 |
|---|---|---|
| `shell` | 29×56×29 | 둥근 테두리 8층. 서쪽에 현관 |
| `core` | 21×56×21 | 통짜 돌 + 칸마다 직소 하나 |
| `cross` `tee` `straight` `corner_left` | 7³ | 손으로 지은 통로 |
| `corner_right` | 7³ | `corner_left`의 거울 |
| `cross_guard` `tee_guard` | 7³ | 같은 것에 스포너(벡스 / 좀비 주민) |
| `dead_end` | 7³ | 보상 방. 문 맞은편에 상자 |
| `sealed` | 7³ | **비밀방.** 문 없음, 직소는 벽 속 |
| `stair` | 7×14×7 | 손으로 지은 나선. 두 층 |
| `library` | 14×7×14 | 4층. 유효 책장 15 + 마법 부여대 |
| `sanctum` | 14×7×14 | 8층. 같은 벽을 심층암으로 갈아입힘 |

`library`와 `sanctum`은 **같은 손그림**(`room4`)에서 나온다. 그 그림의 문이 칸 중앙에서 한
칸 어긋나 있어서 면을 메우고 중앙에 다시 뚫는다 — 어느 면에 문이 있느냐는 설계고 어디쯤이냐는
아니다.

보스 보상(확정): **엔더의 눈 4 + 불사의 토템 + 마법 부여(25~30) 다이아 장비 1**.

### 검사는 조립해서 걸어 본다

`generate_tower.verify()`가 **게임이 하는 대로** 조립한다 — 자식 직소가 부모 직소에 맞닿고
서로 마주 볼 때까지 돌린다. 그게 바닐라 배치의 전부다. 그 결과를 현관에서 flood-fill해서
**72칸 전부 도달 가능**해야 통과한다.

## 마녀의 늪 (`structure/swamp/`)

0.6.0. 늪·맹그로브 늪, spacing 36 / separation 12. **물 위 말뚝 마을과 그 밑 양조장.**
전부 `tools/generate_swamp.py`가 만든다. 층별 단면도는 `docs/levels/swamp.md`.

```
y  4~10   오두막과 널다리
y     3   덱
y −9~ 2   물속 말뚝 — 여기는 말뚝 말고 아무것도 쓰지 않는다 (큰 오두막은 0~2, 그 밑은 pilings)
y−10~−30  수직통로 (여기는 공기를 일부러 쓴다: 물을 밀어내야 한다)
y −31~    진흙 벽돌 양조장 미로 + 보스 사슬 셋 + 늪의 어머니 (21×14×21)
```

**위아래가 서로 다른 기계다.** 물 위는 마을처럼 자라고(형태를 지킬 것도 채울 칸도 없다),
물 밑은 지하감옥의 기계다. 보스만 단일 원소 풀 사슬로 건다 (§7).

시그니처(확정): **양조기 + 네더 사마귀 8 + 블레이즈 가루 4.** 네더에 가기 전에 양조가
열리는 유일한 길이다.

### 17. 물 위에 놓는 조각은 덱 아래를 쓰지 않는다

늪의 구조물은 물 위 첫 빈 칸에서 시작하므로(`WORLD_SURFACE_WG`) 덱을 세 켜 올리고 말뚝을
내린다. **덱 아래는 말뚝 말고 아무것도 쓰지 않는다** — 안 쓴 자리는 월드가 갖고 있던 것이
그대로 남는다(§2). 공기로 쓰면 오두막마다 밑에 마른 구멍이 파인다.

수직통로는 정반대로 **일부러 블록을 쓴다.** 지나가는 물을 밀어내야 하기 때문이다.
사다리 한 칸을 **판자 여덟 칸이 둘러싸고**(3×3), 그 케이싱이 늪을 막는다 (§33). 사다리는
가운데에 서고 직소는 그 동쪽 기둥에 선다. 네 조각(큰 오두막·받침·수직통로·허브)이 같은
좌표를 써야 하므로 `HOLE` 하나에서 나온다 (§11).

### 18. 시작 조각은 바닥이 고정된다. 그 아래로는 자식을 매단다

`JigsawPlacement`는 시작 조각을 **`minY + 1`이 지표**가 되도록 옮긴다. 그래서 시작 조각은
제 바닥 아래로 아무것도 내릴 수 없다 — 조각을 아래로 늘리면 말뚝이 길어지는 게 아니라 구조물
전체가 그만큼 **떠오른다.** 자식은 반대로 **직소의 높이만으로** 놓이므로 얼마든지 깊어도 된다.

늪이 그래서 둘로 나뉜다. 큰 오두막(시작)은 11칸 그대로고 말뚝이 세 칸, 나머지 마을 조각은
바닥이 덱보다 12칸 아래라 말뚝이 거기까지 내려간다. 큰 오두막 밑은 `pilings`(21×9×21)가
기둥과 사다리를 이어받고, 수직통로는 그 밑에 붙는다. **말뚝이 짧아서 마을이 공중에 그루터기로
서 있었다**(2026-09-21 스크린샷).

`terrain_adaptation`은 **`none`**이다. 말뚝으로 서는 것이라 받칠 것이 없고, 받치면 늪이 메워진다.

문은 덱 위로 y+1~y+4, 직소는 덱과 같은 높이다. `verify()`가 두 덱 높이와 "덱 아래에 말뚝 말고
다른 것이 쓰였는지", 그리고 **사다리가 덱에서 지하 바닥까지 끊기지 않는지**를 검사한다 —
네 조각이 각자 제 직소로 매달려 한 줄을 이루므로, 그 줄에 직소 하나만 서 있어도 발판이
빠진다 (한 번 그랬다).

## 얼음 성채 (`structure/ice/`)

0.7.0. 눈 여섯 종(눈 평원·얼음 가시·눈 타이가·눈 비탈·서리 봉우리·그로브), spacing 36 /
separation 12. **49×49 성벽 안의 아성과 그 밑 얼음 창고.** 전부 `tools/generate_ice.py`가
만든다. 층별 단면도는 `docs/levels/ice.md`.

```
        14        21        14        keep   21×21×21  시작 조각, 아성 3층
   ┌────────┬──────────┬────────┐     panel  21×25×14  안뜰 7 + 성벽 7 + 기초 7
14 │ corner │   wall   │ corner │     tower  14×25×14  귀퉁이 탑 (거울 한 쌍)
   ├────────┼──────────┼────────┤     gate   21×25×14  서쪽 성문
   │  wall  │   keep   │  wall  │ 21
   ├────────┼──────────┼────────┤     아성 바닥 → 수직통로 14 → 얼음 창고 미로
14 │ corner │   gate   │ corner │     → 통로 셋 → 서리 군주 (21×14×21)
   └────────┴──────────┴────────┘
```

시그니처(확정): **마법 부여(25~30) 다이아 방어구 1 + 서리 걷기 II 책.** 서리 걷기는 마법
부여대에서 안 나오는 보물 마법이라 이 책이 유일한 길이다.

**재질은 위아래가 다르다.** 안뜰 위는 **윤나는 섬록암**(주재료)과 **석영 벽돌**(테·창틀·총안),
기초는 **조약 심층암**, 안뜰 밑은 **심층암 타일**이다. 눈 위에 서는 성이라 흰 돌로 짓고, 지하는
어두워서 얼음이 살아난다. 조각은 돌벽돌로 지어도 되고 `repalette()`가 한 곳에서 바꿔 준다 —
가족을 통째로 갈아입히는 일을 블록 하나씩 할 필요는 없다.

### 19. 가두고 싶지 않은 가지는 상자 안 조각의 자손이면 안 된다

성벽을 49×49 한 조각으로 만들면 아성이 그 상자 안에 매달리고, §10의 울타리가 상속되어
**지하 창고까지 성벽 안에 갇힌다.** 그래서 성벽은 가운데가 빈 여덟 조각(벽 넷 + 탑 넷)이고,
아성이 시작 조각이며, 내려가는 직소는 아무것도 가두지 않은 자리에서 뻗는다.

지상은 전부 결정적이다 (§11): 아성이 사방으로 벽을 부르고, **북벽과 남벽만** 제 양 끝에서
탑을 부른다. 둘만 부르는 이유는 탑이 4분면 중 한 곳에만 두꺼운 모서리를 갖기 때문이다 —
동·서벽도 부르면 같은 탑을 두 방향에서 불러 **먼저 놓이는 쪽에 따라 탑이 안뜰로 들어온다.**
한 번만 부르면 회전이 하나로 정해진다 (§13). 바닐라는 돌려는 줘도 뒤집어 주지 않으므로
북서용과 북동용은 거울 한 쌍이다.

산 바이옴이라 바닥이 평평하지 않다. `beard_thin` 위에 **벽과 탑이 기초를 일곱 칸 아래로**
끌고 내려간다 (§18). 안뜰은 조각이 덮으므로 평평하게 깎인다.

### 20. 손으로 고친 것은 SAVE 없이 가져온다

`workshop.py`가 깔아 준 구조물 블록은 **블록엔티티**라서 조각 이름과 상자 크기를 제 안에
갖고 있다. `tools/grab_workshop.py`는 월드의 region 파일에서 그걸 찾아 **그 상자 안의 블록을
그대로 읽어** `tools/handmade/<던전>/`에 쓴다 — 게임에서 SAVE를 누를 필요가 없다. 사람이
잊는 단계를 없앤 것이다.

돌아오는 것은 **생김새**뿐이다. 직소는 두고 오고(배선은 코드 것, §16), 생성기가 제 것을 다시
찍는다. 상자·스포너처럼 **NBT를 지닌 것도 코드 것**이다 — 게임 안에서는 전리품 테이블을
붙일 방법이 없으므로, 코드가 놓은 가구는 사람이 그 자리를 비워 뒀으면 도로 찍히고, 작업장에서
**열어 본 상자는 테이블 대신 굴린 결과를 갖고 돌아오므로 아예 버린다.** 상자를 어디에 놓고
싶으면 코드에 말해야 한다.

생성기는 `tools/handmade/ice/<이름>.nbt`가 있으면 **그것을 쓰고** 없으면 제가 짓는다.
상자 크기가 다르면 거부한다 — 상자만은 바꿀 수 없다.

### 21. 조립해서 걸어 본다

`generate_ice.walk_problems()`가 **쓰고 난 파일을 다시 읽어** 게임처럼 조립하고, 아성 한복판에서
걸어서 안뜰·성문·양쪽 순찰로·탑 꼭대기·탑 바닥·지하까지 닿는지 본다. 두 번 값을 했다:
탑에 순찰로 높이의 바닥이 없어서 **문으로 들어서면 열다섯 칸을 떨어졌고**, 안뜰에서 성벽으로
오르는 계단이 아예 없어서 **고리가 보기만 하는 물건**이었다. 마법사의 탑의 flood-fill과 같은
생각이다.

`fitting_problems()`는 같은 눈으로 조각 하나하나를 본다: **공중에 뜬 블록**, 뒤에 벽이 없는
사다리, 매달 데 없는 랜턴, 테이블 없는 상자, 허공에 놓인 상자. 손으로 고친 조각이 돌아올
때마다 돌아야 하는 검사다 — 사람은 블록을 지우고 그 위의 것을 잊는다.

### 22. 가구는 좌표가 아니라 **빈자리를 찾아** 놓는다

`furnish()`는 "여기에 모루"가 아니라 "이 구역에서 **비어 있고, 아래가 단단하고, 머리 위가
트인** 첫 자리에 모루"라고 쓴다. 그래서 손으로 다시 지은 방에서도 가구가 알아서 들어갈 자리를
찾고, **공중에 뜨는 일이 없다.** 아성 세 층은 네 귀퉁이를 각각 화덕·무기고·창고·서재로 쓰고,
문은 네 면 가운데에 있으니 귀퉁이만 쓰면 길을 막지 않는다.

## 덩굴에 삼킨 신전 (`structure/temple/`)

0.8.0. 정글 세 종(정글·성긴 정글·대나무 정글), spacing 36 / separation 12.
**밑변 63, 높이 35의 계단식 지구라트**고 반은 지하다. 전부 `tools/generate_temple.py`가 만든다.
층별 단면도는 `docs/levels/temple.md`.

```
skin_base 63×14×63  ─안쪽→ core      49×7×49   마른 미로 + 우물까지의 스파인 셋
                    ─안쪽→ core_deep 49×7×49   물살 수로
skin_mid  49×21×49  ─안쪽→ vault     21×14×21  환영술사 사제의 홀
shrine     7× 7× 7                             꼭대기. 대계단으로 밖에서 올라간다
```

재질은 **이끼 조약돌 + 이끼 벽돌 + 정글 나무**, 실루엣 밖은 안 쓴다(§2). 대계단은 남면,
들어가는 문은 북면 — 같은 면에 두면 서로를 파낸다.

**`start_height`가 −7이다** (§32). 기초 일곱 켜가 땅속으로 들어가서 입구와 대계단이 정글
바닥에서 시작한다. 그전에는 둘 다 일곱 칸 공중에 있었다.

대계단은 **네 칸 깊이로 판다.** 세 칸이면 디딤판 위 공기가 둘뿐이라 걸을 수는 있어도
점프하면 천장에 머리를 박는다. 그리고 `stair_cut`은 **temple y < 1이면 아무것도 안 판다** —
`y % STEP`이 음수 y에서도 돌아서 기초에 계단이 한 벌 더 새겨졌고, 그게 땅바닥에 뚫린
가짜 입구로 보였다.

시그니처(확정): **마법 부여(25~30) 다이아 무기 1 + 행운 III 책 + 에메랄드 8~14.**

### 23. 지하가 있으면 기초를 껍데기에 넣는다

울타리는 상속되므로(§19) 한 번 상자 안에 들어가면 그 밖으로는 못 나간다. 그래서 **지하를
따로 매달 수 없다** — `skin_base`를 14켜로 만들어 일곱 켜를 정글 바닥 아래에 두고, 마른 미로와
젖은 미로를 **둘 다 그 하나의 상자 안에** 매달았다. 피라미드가 지상만 있어서 필요 없던 것이다.

### 24. 사다리에는 발판이 필요하다 → **§33이 대신한다**

3×3 구멍 한가운데로 사다리가 지나가면 걸어서 올라탈 수가 없어서, 층마다 사다리가 있는 줄을
발판으로 남겼다. **구멍이 한 칸이 된 뒤로는 필요 없다** (§33) — 구멍이 곧 사다리라 걸어
들어가면 잡힌다. 발판 규칙은 전부 뺐다.

문턱은 여전히 같은 이야기다. 사당은 테라스보다 한 칸 높으므로 문을 **바닥 켜부터** 파야 마지막
한 걸음이 한 칸이 된다. 두 칸이면 걸어 올라갈 수 없다.

## 군벌의 진영 (`structure/camp/`)

0.9.0. 사바나 셋과 악지 셋, spacing 40 / separation 14. **49×49 목책 진영과 그 밑 갱도.**
전부 `tools/generate_camp.py`가 만든다. 층별 단면도는 `docs/levels/camp.md`.

```
longhouse 21×21×21  시작 조각. 대말움집, 바닥에 갱도로 가는 구멍
panel     21×21×14  목책 두 겹(5칸) + 마당 12 + 기초 7
tower     14×28×14  귀퉁이 감시탑. 발판을 뚫고 오르는 사다리 (거울 한 쌍)
gate      21×21×14  서쪽 문
→ 사다리 21칸 → 갱도 허브 → 나무로 받친 갱도 + 보스 사슬 셋 → 군벌 (21×14×21)
```

고리는 얼음 성채와 같은 기계다 (§19) — 다른 것은 **목책이 두 칸밖에 안 두꺼워서** 마당이
탑까지 닿고, 그래서 탑 문이 땅 높이에 있다는 점이다. 악지는 한 구조물 안에서 지형이 무너지므로
조각마다 기초 일곱 켜를 단다 (§18).

시그니처(확정): **다중 발사 또는 관통 석궁 + 불길한 병 2~3 + 에메랄드 10~18 + 파수꾼 장식
템플릿 2.** 불길한 병은 바닐라 트라이얼 챔버를 불길하게 여는 열쇠다.

**라버저 우리는 트리거가 아니라 선택이다.** 철창 안에 스포너가 있고 몹은 철창을 깨지 않으므로,
그 방이 위험해지는 것은 플레이어가 깨기로 마음먹을 때뿐이다 (§12).

## 저주받은 묘지 (`structure/grave/`)

0.10.0. 어두운숲과 창백한 정원, spacing 40 / separation 12. **예배당에서 퍼지는 묘역과 그
밑 납골당.** 전부 `tools/generate_grave.py`가 만든다. 층별 단면도는 `docs/levels/grave.md`.

```
chapel  21×21×21  시작. 예배당 폐허. 사방 문이 묘역을 부르고 바닥에 구멍
plot    7×14×7    묘역 여덟 종(묘비·파헤쳐진 무덤·석실·죽은 나무·제단·갈림길)
plot_cap 1×14×7   철 울타리 한 장. 묘역 풀의 fallback
→ 사다리 28칸 → 납골당 허브 → 바위를 파낸 납골당 + 보스 사슬 셋 → 기병대장 (21×14×21)
```

**고리가 아니라 퍼지는 쪽이다** — 늪의 널다리와 같은 기계다 (§17). 성채·진영이 담을 닫는
쪽이고 묘지는 자리 있는 데로 뻗는 쪽이다. 지하는 §5.2대로 **돌과 안산암 띠**로 짓고 돌벽돌·
뼈·양초는 안쪽에만 둔다.

시그니처(확정): **수선(Mending) 책 + 안장 + 마법 부여(20~28) 다이아 장비 1.**
**수선은 이 모드에서 여기밖에 없다** — 마법 부여대는 보물 마법을 주지 않으므로, 파밍 없이
장비를 영구히 쓰는 길이 이 던전 하나다.

### 26. 트라이얼 스포너가 보상을 죽인다

콘셉트는 "보스를 잡으면 길들여진 스켈레톤말이 남는다"였는데, **트라이얼 스포너는 제가 낸 몹이
다 죽어야 끝난다.** 보스를 말에 태우면 말도 죽여야 클리어고, 그 말이 보상이다.

그래서 대장은 걸어 나오고, 같은 홀에 **평범한 몬스터 스포너**로 `Tame:1` 스켈레톤말을 둔다 —
트라이얼이 세지 않는 몹이라 싸움이 끝나도 서 있고, 보상의 안장으로 타고 나갈 수 있다. 살아
남아야 하는 것은 트라이얼 밖에 둔다.

## 드워프의 전당 (`structure/dwarf/`)

뾰족·돌 봉우리와 바람 언덕 셋, spacing 40 / separation 14. **지상에 문 하나, 그 아래
42칸에 전당 전부.** `tools/generate_dwarf.py`가 만든다. 층별 단면도는 `docs/levels/dwarf.md`.

```
gatehouse 21×24×21  시작. 심층암 문간, 북면에 아치 큰 문, 바닥에 구멍
→ shaft_1 · shaft_2 (7×21×7씩, 단일 원소 풀로 엮임) → hall_hub
→ 돌을 파낸 전당 미로 + 보스 사슬 셋 → 대장장이 왕 (21×14×21)
```

기계는 묘지 그대로다. **다른 것은 지상 조각이 하나뿐이라는 것이다** — 봉우리는 게임에서
가장 급한 지형이라 퍼지는 구조는 죽마 위에 서거나 산에 파묻힌다. 계단 조각은 아예 없어서
미로가 산 옆구리로 되솟지 못하고(§5.1), 껍데기는 §5.2대로 **돌에 화강암 띠**다.

시그니처(확정): **효율 IV·내구성 III 다이아 곡괭이 또는 도끼 1 + 갑옷 장식 템플릿 2 + 모루.**

**바람 언덕 셋을 지하감옥의 태그에서 뺐다** — `docs/concepts.md` §4.0이 전당 몫으로 적어
두고 "전당을 만들 때 빼라"고 메모해 둔 것이다.

### 28. 새는 용암은 영원히 샌다

용광로의 첫 판은 조각 **바닥면**에 용암을 깔았다. 템플릿은 그것을 그대로 놓고, 그 아래는
다른 조각이거나 그냥 돌이므로 **월드가 사는 동안 계속 흘러내린다.** 지금은 바닥 한 칸 위,
응회암 테두리 안에 있다.

`lava_problems()`가 용암 블록마다 **옆 넷과 아래**가 막혀 있는지 본다. 위는 보지 않는다 —
용암은 위로 흐르지 않고, 구렁의 용암은 위가 열려 있어야 구렁이다.

같은 검사가 떠 있는 광석도 잡았다. `vein`은 빈 방에 광석을 뿌린 것이었는데, 방 안에 뿌리면
광석은 떠 있는 것이 맞다 — 그래서 칸을 돌로 되메우고 굴만 판다.

## 균사 동굴 (`structure/grove/`)

버섯섬, spacing 20 / separation 8. **버섯 넷이 둘러싼 구멍과 그 아래 균사 굴.**
전부 `tools/generate_grove.py`가 만든다. 층별 단면도는 `docs/levels/grove.md`.

```
mouth  7×7×7    시작. 균사체에 뚫린 구멍, 커다란 버섯 넷. 지상에 있는 것은 이게 전부다
→ 사다리 14칸 → hollow → 균사 미로 + 이름이 다른 단일 원소 풀 둘(pen · camp) → 1칸 마개
```

**이 모드의 첫 중소규모다** (`docs/concepts.md` §2.8). 기계는 지하감옥을 짧게 줄인 것이고,
다른 것은 **주는 것이 없다는 것**이다 — 시그니처도 보스도 없고, 무무 목장과 침대 둘이 있는
야영지가 전부다.

### 25. 작은 던전은 트라이얼 스포너를 갖지 않는다

§7이 "보스는 던전마다 하나"인데, 그것은 **대규모 던전마다** 하나다. 작은 던전에도 트라이얼
스포너를 달면 §2.4(마법 부여 장비는 보스당 한 조각)가 무너진다 — spacing 20이면 걷다가
걸리는 것이 목적이라, 풀세트가 "승리 여섯 번"에서 한나절이 된다.

그래서 `generate_grove.verify()`와 `generate_shrine.verify()`는 **트라이얼 스포너가 하나라도
있으면 거부한다.** 규칙을 문장으로 적어 두는 것과 검사기가 막는 것은 다르다.

## 산신의 사당 (`structure/shrine/`)

벚꽃숲, spacing 40 / separation 14. **토리이가 선 네 갈래 돌길과 그 가운데 마당·단상·사당.**
전부 `tools/generate_shrine.py`가 만든다. 층별 단면도는 `docs/levels/shrine.md`.

```
court  21×23×21  시작. 담을 두른 마당, 네 면에 토리이와 문, 가운데 일곱 칸 단상 위 제단
cellar 7×7×7     그 단상 **속**. 문이 하나도 없다 (§15)
path   7×14×7    길 여덟 종(토리이·쉼터·정원·온천·등불·직선·꺾임·네거리) + 1칸 마개
```

**이 모드에서 상자가 진행을 주는 단 하나의 자리다.** 제단 상자에 **엔더 진주 6 + 경험치 병
8**이 확정으로 들어 있다 — §2.1(진행 필수품을 상자에 두지 않는다)의 유일한 예외이고,
`docs/concepts.md` §4.11에 예외로 적혀 있다. 적이 없고 스포너도 없으니 싸울 것이 없는
대신, 주는 것이 장비가 아니라 재료다.

### 27. 자식은 부모의 직소 **앞**에 놓인다. 박스 밖으로 나가면 말없이 사라진다

지하실을 단상 **서쪽** 벽에 걸었더니 마당 밖으로 한 칸 밀려났다. 한 칸이면 충분하다 —
부모의 박스에 들어가지 않는 자식은 버려지고(§10), 로그도 남지 않는다. **지하실이 아예
생성되지 않았고, 조각을 아무리 뜯어 봐도 알 수 없었다.**

직소는 자식이 놓일 자리를 **가리키는** 것이다. 그래서 단상 속에 방을 넣으려면 직소가
반대쪽(동쪽) 벽에 서서 단상을 관통해 되돌아봐야 한다. `cellar_problems()`가 그 계산을
그대로 해서 지하실이 실제로 떨어지는 좌표를 찍어 본다 — 단상 안이 아니면 거부한다.

산을 오르는 계단 사슬을 포기한 이유도 같은 종류다. 사슬의 각 조각은 지형이 아니라 **자기
직소의 높이로** 놓이므로(§18), 경사가 딱 맞는 지형 하나 말고는 마지막 계단이 공중에 뜬다.
오르막은 마당 안의 단상으로 들여왔다 — 기하가 우리 것이면 계단은 항상 닿는다 (§11).

그리고 손으로 깎은 계단은 **조용히 계단이 아니게 된다.** 첫 단상 계단은 디딤판을 단상 속에
써 넣은 것이어서 위에 공기가 없었고, 제단으로 올라갈 길이 없었다. `walk_problems()`가 네
문에서 제단까지 플레이어처럼 걸어 본다 — 발밑이 단단하고, 머리 위 두 칸이 비고, 한 번에
한 칸 오르고 두 칸 내린다.

## 침몰한 등대 (`structure/light/`)

해변·눈 덮인 해변·자갈 해안, spacing 34 / separation 12. **부서진 등대와 그 아래 물에 잠긴
요새.** `tools/generate_light.py`가 만든다. 층별 단면도는 `docs/levels/light.md`.

```
lighthouse 7×30×7  시작. 7×7뿐이다 — 더 넓으면 해변이 제 돌바닥으로 덮인다
→ shaft 7×21×7 (마름) → sump (마른 집수실, 바닥에 구멍)
→ dive 7×14×7 (물기둥, 맨 윗 켜만 공기) → sea_hub → 잠긴 프리즈머린 요새
→ 보스 사슬 셋 → 심해의 파수꾼 (21×14×21, 물 여섯 켜 + 그 위 공기)
```

**먼바다에는 못 놓는다.** 구조물 하나는 높이 기준을 하나만 쓰는데, 등대가 모래에 서려면
`WORLD_SURFACE_WG`여야 하고 바다에서 그 값은 수면이다. 바다 던전은 `OCEAN_FLOOR_WG`를 쓰는
다른 구조물이어야 한다.

시그니처(확정): **삼지창(충성 III 또는 급류 II) + 해양의 심장 + 해면 2~5 + 수중 마법 책 하나.**

### 29. 놓은 물은 가둬야 한다. 마른 쪽과는 수면에서 만난다

늪에서 배운 절반은 "안 쓴 자리는 월드의 물이 남는다"였다 (§17). 요새는 나머지 절반이다 —
**물을 직접 놓아야 하고**, 놓은 물은 드워프의 용암과 똑같이 갇혀 있어야 한다 (§28).

`water_problems()`는 물 블록마다 **옆 넷과 아래**에 공기가 없는지, 그리고 조각의 면에 있는
물이 전부 그 조각이 기록해 둔 구멍(`ports`)인지 본다. **위는 보지 않는다** — 그게 수면이다.
`door_problems()`는 잠긴 칸의 문간 3×4가 전부 물인지 본다. 두 칸은 **물과 물로** 만나야
하고, 한쪽이 공기면 둘 다 빠진다. fallback 마개가 통짜인 것도 그래서 중요하다.

마른 쪽과 젖은 쪽의 이음매는 **수직**이다. 물은 위로 흐르지 않으므로, 집수실 바닥의 구멍
아래 두 칸에 수면을 두면 그것으로 끝이다 — 막아 줄 문도 유리도 없다.

**조립 검사도 이 던전에서 헤엄을 배웠다.** 다른 던전의 걷기 검사는 "발밑이 단단할 것"을
요구해서 떨어지는 것도 헤엄치는 것도 못 했고, 그것을 풀었더니 이번에는 **조각 밖의 빈
좌표로 새어 나가 끝없이 퍼졌다.** 조각이 없는 자리는 월드의 땅으로 치는 것이 맞다 —
모르는 좌표는 단단한 것으로 센다.

## 중소규모 여덟 (`structure/{well,tomb,cabin,cairn,dryw,canopy,lodge,mire}/`)

0.12.0. **한 기계, 바이옴마다 껍질 하나.** 전부 `tools/generate_small.py`가 만든다.
층별 단면도는 `docs/levels/well.md` 외 일곱 — 같은 원고에서 껍질마다 채워진 것이다.

```
head  7×8×7   입구 네 종(우물·둔덕·오두막·돌무덤) 중 하나. 바닥 가운데 한 칸이 구멍
→ shaft 7×21×7 → hub → 미로 다섯 종 + store(상자 방) → 1칸 마개
```

| 껍질 | 바이옴 | 비껴 서는 대규모 |
|---|---|---|
| `well` 숨은 우물 | 평원·숲 7 | dungeons |
| `tomb` 모래 아래 무덤 | 사막 | pyramids |
| `cabin` 사냥꾼의 오두막 | 타이가 3 | towers |
| `cairn` 눈 속 돌무덤 | 눈 6 | fortresses |
| `dryw` 마른 우물 | 사바나 3 + 악지 3 | camps |
| `canopy` 덩굴 밑 저장고 | 정글 3 | temples |
| `lodge` 숲지기의 지하실 | 어두운숲·창백한 정원 2 | graveyards |
| `mire` 물에 잠긴 저장고 | 늪·맹그로브 2 | swamps |

### 35. 같은 모양을 여덟 번 쓰지 않는다 — 표가 껍질을 말한다

작은 던전은 지하감옥의 숫자를 줄인 것이다. 여덟 번 따로 쓰면 같은 버그를 고칠 자리가 여덟
개가 된다. 그래서 모양은 한 번만 쓰고 `SKINS` 표가 **무엇으로 지었는지(rock·band·brick·
trim·floor·wood) · 어디 서는지(biomes) · 무엇이 사는지(mob) · 어느 대규모를 비껴 서는지
(avoid)**만 말한다. 조각·풀·전리품·바이옴 태그·structure·structure_set·processor가 전부
그 표에서 나온다. 레벨 문서의 원고도 하나다 (`level_notes_small.py`).

껍질을 하나 더 넣는 일은 표에 줄 하나를 더하는 일이다.

### 36. 작은 던전과 큰 던전은 `exclusion_zone`으로 떼어 놓는다

같은 바이옴을 나눠 쓰므로 가끔 같은 자리에서 만난다. 바닐라 `random_spread`의
**`exclusion_zone`**이 그것을 막는다 — `{"other_set": "sydungeon:dungeons", "chunk_count": 6}`
이면 그 세트가 있는 6청크 안에는 서지 않는다. 우선권은 먼저 계산되는 쪽이 아니라 **다른
세트를 가리킨 쪽이 물러나는 것**이므로, 물러나는 쪽은 언제나 작은 던전이다.

## 원본과 도구

원본 연습 조각은 월드 `C:\Users\syang\AppData\Roaming\.minecraft\saves\던전`의 `generated/yame/structure/`에 있고, **그 월드는 건드리지 않는다.** 읽기 전용 스냅샷이 `tools/orig_yame/`에 있다.

| 도구 | 하는 일 |
|---|---|
| `tools/nbt.py` | 의존성 없는 NBT 읽기/쓰기. 원본을 바이트 단위로 되살린다 |
| `tools/validate_data.py` | **빌드 전에 돌린다.** 데이터팩의 모든 id를 게임 jar와 대조 + 상호 참조 검사 (§9) |
| `tools/check_starts.py` | 시작 조각이 제 기초 위에 솟아 있지 않은지 (§30). `validate_data.py`가 부른다 |
| `tools/check_shafts.py` | **직소로 조립해서** 내려가는 길을 타 본다 (§33). `validate_data.py`가 부른다 |
| `tools/make_pieces.py` | 아래 셋을 순서대로. **조각을 다시 만들 때는 이것 하나만 돈다** |
| `tools/convert_yame.py` | 스냅샷 → `structure/dungeon/*.nbt` (이름 변경, 직소 name/target/pool 치환, 26.3→26.2 팔레트 내림) |
| `tools/generate_pieces.py` | 감옥의 cap · shaft_cap · entrance · shaft · hub · boss_room을 코드로 생성, boss_passage는 cross에서 파생 |
| `tools/generate_pyramid.py` | 대피라미드 전체(껍데기·스파인·묘실·미로·함정)와 그 풀 JSON. 기하 자체 검사 포함 |
| `tools/generate_swamp.py` | 마녀의 늪 전체(말뚝 마을·수직통로·양조장·보스 사슬)와 그 풀 JSON |
| `tools/generate_grove.py` | 균사 동굴. 보스도 시그니처도 없다. 트라이얼 스포너를 거부하는 검사 포함 |
| `tools/generate_small.py` | **중소규모 여덟.** 한 기계에 `SKINS` 표, 조각부터 전리품·태그·세트까지 (§35) |
| `tools/generate_shrine.py` | 산신의 사당. 지하실이 단상 안에 떨어지는지, 네 문에서 제단까지 걸어지는지 검사 |
| `tools/generate_dwarf.py` | 드워프의 전당. 사다리 42칸과 용암 누출 검사, 조립 후 걸어 보기 |
| `tools/generate_light.py` | 침몰한 등대. 물 누출·문간 검사, 조립 후 걸어 보고 헤엄쳐 보기 |
| `tools/generate_ice.py` | 얼음 성채 전체(아성·성벽·탑·얼음 창고)와 그 풀 JSON. 조립 후 걸어 보는 검사 포함 |
| `tools/generate_temple.py` | 정글 신전 전체(테라스·두 미로·수로·사제의 홀)와 그 풀 JSON. 기하·조립·걸어 보기 검사 포함 |
| `tools/generate_camp.py` | 군벌의 진영 전체(움집·목책·감시탑·갱도)와 그 풀 JSON. 같은 검사 셋 포함 |
| `tools/generate_grave.py` | 저주받은 묘지 전체(예배당·묘역·납골당)와 그 풀 JSON. 같은 검사 셋 포함 |
| `tools/generate_tower.py` | 마법사의 탑. 손으로 지은 조각을 배선하고 테두리를 쌓는다. 조립 후 flood-fill 검사 포함 |
| `tools/tower_layout.py` | 그 탑의 72칸 배치를 푸는 동적 계획. 경로·계단·큰 방 자리 |
| `tools/scan_world.py` | 월드 region에서 블록을 그대로 읽는다. 사람이 놓은 것과 지형을 가른다 |
| `tools/grab_cells.py` | 월드에 그린 층 배치도를 격자대로 잘라 조각으로 만든다 |
| `tools/gen_level_doc.py` | `docs/levels/*.md`와 조각별 층 단면도 SVG를 만든다. **조각을 고쳤으면 돌린다** |
| `tools/level_notes.py` | 그 문서의 설명 문장. 도구가 아니라 원고다 |
| `tools/simulate.py` | 풀 가중치로 던전 크기·계단 수·층 수 분포를 종이 위에서 굴려 본다 |
| `tools/inspect_world.py` | 개발 월드 region 파일에서 실제 생성된 던전의 조각·층 분포를 읽는다 |
| `tools/gen_logo.py` | CurseForge 로고 (`docs/curseforge/logo.png`, `src/main/resources/logo.png`) |
| `tools/decorate.py` | cell과 dead_end에 상자·스포너 NBT 삽입 |
| `tools/dump_structure.py` | 조각을 층별 텍스트로 출력 + 직소 목록. 26.2/26.3 팔레트 둘 다 읽음 |
| `tools/pack_datapack.py` | `data/`를 바닐라 데이터팩 zip으로 묶음. 모드 빌드 없이 26.2 월드에서 `/place`로 확인 |
| `tools/workshop.py` | 개발 월드에 작업장 데이터팩을 깐다. `/function workshop:tower` 한 번이면 모든 조각이 **SAVE 설정된 구조물 블록과 함께** 바닥에 깔린다 |
| `tools/grab_workshop.py` | 작업장 월드에서 **SAVE 없이** 조각을 읽어 `tools/handmade/<던전>/`에 쓴다 (§20) |
| `tools/import_piece.py` | 손으로 고친 조각을 모드로 되가져온다. **모드에 있는 같은 이름 조각을 계약으로 삼아** 크기·직소(위치/향/name/target/pool/joint)·문 구멍이 그대로인지 대조한다 |

### 16. 어떻게 생겼는지는 사람 것이고, 어떻게 배선됐는지는 코드 것이다

코드로 만든 조각은 **출발점**이다. 예쁘게 만드는 일은 개발 클라이언트에서 손으로 한다 (§1).
마법사의 탑이 그 증거다 — 0.3.0과 0.4.0은 코드가 지었고 둘 다 퇴짜를 맞았다. 0.5.0은
사람이 층 배치도를 그렸고 코드는 자르고 배선만 했다.

코드가 지키는 것은 직소(위치·향·name·target·pool·joint)와 상자 기하뿐이고, `import_piece.py`가
**모드에 이미 있는 같은 이름 조각을 계약으로 삼아** 대조한다. 벽지·가구·조명·방 모양은 전부 자유다.

**구조물 블록은 한 변 48칸이 한계다.** 탑에서 그 안에 드는 것은 전부다 —
테두리 한 층(29×7×29), 큰 방(14×7×14), 칸(7³), 계단(7×14×7). 못 드는 것은 다 합친
`shell`(29×56×29)과 `core`뿐이고, 그 둘은 코드가 조립한다.

월드에 그린 것을 되가져오는 길이 둘 있다. 구조물 블록으로 저장했으면 `import_piece.py`,
저장 안 했으면 `scan_world.py`가 region 파일에서 직접 읽고 `grab_cells.py`가 격자대로 자른다.
**후자는 블록 상태까지 가져와야 한다** — 한 번 이름만 가져와서 손으로 지은 나선 계단이
전부 북쪽만 보는 계단 더미가 됐다.

## 조각 만들기 (개발 클라이언트)

`./gradlew runClient`로 뜨는 게임이 곧 26.2 + 이 모드다. 조각은 거기서 만든다.

```bash
python tools/workshop.py tower      # 개발 월드마다 작업장 데이터팩을 깐다
```

1. 크리에이티브 월드에서 빈 곳으로 날아가 `/reload` 후 `/function workshop:tower`.
   조각이 전부 동쪽·남쪽으로 깔리고, 각각 **자기 이름으로 SAVE 설정된 구조물 블록**이
   왼쪽 아래 모서리에 붙는다 (경계 상자 표시 켜져 있음)
2. 고친다. 7×7×7 셀 규약(§2)과 직소는 건드리지 않는다
3. 그 조각의 구조물 블록에서 **SAVE** →
   `run/saves/<월드>/generated/sydungeon/structure/tower/<이름>.nbt`
4. `python tools/import_piece.py tower/<이름>` — 모드에 있던 것과 배선을 대조해서
   직소가 사라졌거나 문이 막혔으면 말해 준다. `--all`로 한꺼번에도 된다
5. `./gradlew build` → 다시 실행. **jar 안의 조각은 재시작해야 바뀐다.** 다만 저장한 조각이
   그 월드의 `generated/`에 있는 한 월드는 그것을 먼저 읽으므로, 그 안에서는 `/place`로 바로 보인다

새 조각을 아예 추가했다면 풀 JSON에 원소를 넣는 것은 사람 몫이다 — 도구가 해 주지 않는다.

## 생성 결과 읽기

"단층만 생겼다"처럼 눈으로 본 것과 통계가 다를 수 있다. `tools/inspect_world.py <월드>`가 개발 월드의 region 파일에서 던전 시작점을 찾아 조각 수·조각별 개수·층별 분포를 출력한다. 던전 하나에 조각 300~1000개, 계단 15~50개, 층 5~8개가 정상이다. 조각 대부분은 hub 층에 몰린다.

## 테스트

```
python tools/pack_datapack.py <월드 경로>     # 바닐라 월드용. 개발 클라이언트에는 필요 없다
/reload
/place jigsaw sydungeon:dungeon/start sydungeon:door 10 ~ ~ ~     # 시작 풀에서 깊이 10
/place structure sydungeon:dungeon                                # structure JSON 그대로
/locate structure sydungeon:dungeon
```

## 코드 규약

- 식별자·주석·커밋 메시지는 영어. 설계 문서는 한국어
- 밸런스 숫자는 JSON(풀 가중치, structure_set 간격)에 있다. Java로 옮기지 않는다
- 로깅은 `SyDungeon.LOGGER`

## 다음

**커버리지를 먼저 메운다** (2026-09-22에 순서를 바꿨다). 육지 바이옴 37종 중 16종만 덮고
있어서 걸어 다니다 던전을 만나는 일이 드물다. `docs/concepts.md` §9:

1. ~~얼음 성채~~ — 0.7.0. 눈 6종. 첫 판이 섰고 손으로 다듬을 차례다
2. ~~덩굴 신전~~ — 0.8.0. 정글 3종. 첫 판이 섰고 손으로 다듬을 차례다
3. ~~군벌의 진영~~ — 0.9.0. 사바나·악지 6종. 얼음 성채의 기계를 아카시아로 썼다

4. ~~저주받은 묘지~~ — 0.10.0. 어두운숲·창백한 정원. **수선**이 드디어 모드 안에 들어왔다

**다음 배포는 1.0.0 하나뿐이다.** 2026-09-22에 정했다 — 0.11.x를 하루에 네 번 밀고 나서다.
**그때까지는 커밋과 푸시만 한다.** 태그를 달지 않으면 릴리스 워크플로가 돌지 않는다.

| 버전 | 내용 |
|---|---|
| 0.12.0 | **중소규모 한 벌** (§2.8). 여덟 껍질이 들어왔다 — 남은 것은 동굴 3종 |
| 0.13.0 | 네더 — 불꽃 요새 · 피글린 금고 · 영혼 성소 (`docs/concepts.md` §5) |
| 0.14.0 | 엔드 — 공허 첨탑 · 코러스 미궁 (§6) |
| **1.0.0** | 구조 설계와 버그를 훑고 **한 번에 배포** |

**한 던전에 한 태그를 민다.** 다 만들고 한 번에 보여 주면 늦다 (§16).

**마법사의 탑에 남긴 것** (커버리지가 끝난 뒤): **지붕은 초안이 섰다** — `generate_tower.roof()`가
처마 한 칸을 내밀고 심층암 타일 원뿔을 14칸 올린 뒤 꼭대기에 피뢰침을 세운다(29×72×29).
코드가 민 것이니 §16대로 손으로 다시 지으면 된다. 테두리 `ring_floor`(29×7×29)도 마찬가지다 —
구조물 블록 한계 안이라 손으로 지어 `tools/handmade/tower/`에 넣으면 생성기가 그걸 쌓는다.
보상 방이 `dead_end` 하나뿐이라 전부 같이 생겼다 — 같은 문 배치의 조각을 더 만들어 풀에
넣으면 되고 **배선은 안 건드려도 된다.**

CurseForge 프로젝트 1703811에 0.1.0·0.1.1·0.2.0·0.5.0·0.6.0을 배포했다. 0.1.1이 감옥
마무리, 0.2.0이 대피라미드다. 마법사의 탑은 세 번 지었다 — 0.3.0(직육면체), 0.4.0(가는 탑),
**0.5.0(사람이 지은 둥근 탑)**. 0.6.0이 **마녀의 늪**이다.
